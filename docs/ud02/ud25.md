## 5.1 Variables

Una variable es un contenedor al que le damos un nombre (identificador) y en el que almacenamos un valor. Se puede almacenar un valor en una variable y consultar este valor posteriormente, y además, el valor de una variable puede cambiar en el tiempo, es decir, el valor puede ser modificado siempre que se desee.

Para declarar las variables en JavaScript se puede utilizar `let` o `var`, según el **ámbito** donde queramos que sea accesible.

- **let**: permite declarar una variable que sea accesible únicamente dentro del bloque donde se ha declarado (llamamos bloque al espacio delimitado por `{ }`).
- **var**: permite declarar una variable que sea accesible por todos los lugares de la función donde ha sido declarada. Si una variable con `var` se declara fuera de cualquier función, el ámbito de ésta son todas las funciones del código.
- **Variables sin declarar**: Javascript permite usar variables no declaradas. Si se hace esto, será equivalente a declararlas con `var` fuera del cualquier función, es decir, serán variables accesibles por cualquier función.

!!! info "Ámbito variables/constantes"
     Indica en qué lugares del programa puede ser accedida una variable/constante

```javascript
function principal() {
    ejemplo=3; // Equivale a declararla fuera de la funcion como var
    if (ejemplo === 3){
        var variable1 = 1;
        let variable2 = 2;
    }
    
    console.log(variable1); // variable1 existe en este lugar
    console.log(variable2); // variable2 no existe en este lugar
}

principal();
```

!!! warning "Importante"
     **Atención:** En general, recomendamos usar **let**. Se debería tener una buena razón para usar **var**, ya que su uso es peligroso porque podría modificarse una variable desde un lugar que no se controle por accidente. Estos problemas no ocurren únicamente en Javascript, sino en otros lenguajes y pueden hacer que el programa sea complicado de depurar.


## 5.2 Constantes

Las constantes son elementos que permiten almacenar un valor, pero ese valor una vez almacenado es invariable (no se puede modificar). Para declarar constantes se utiliza la instrucción `const`. Suelen ser útiles para definir datos constantes, como el número PI, el número de Euler, etc…

Su ámbito es el mismo que el de let, es decir, solo son accesibles en el bloque que se han declarado.

```javascript
const PI=3.1416;
console.log(PI);
PI=3; // Esto falla
```

## 5.3 Convenciones de nombres

Para nombrar una variable o constante sólo podemos utilizar caracteres alfanuméricos `[a-zA-Z0-9]` y el caracter de subrayado `_`. No debe comenzar por un número y tampoco se puede utilizar como identificador una palabra reservada ya que nos daría error. Es conveniente que los nombres, o los identificadores que les damos, sean descriptivos e informen del cometido de dicha variable para que nuestro código sea legible y entendible. También es conveniente seguir la convención `lower camel case` en la que se utilizan minúsculas para nombrarlas y si es la unión de varias palabras éstas se unen y se distingue entre palabras utilizando la primera letra en mayúscula.


## 5.4 Tipos de datos

Los principales tipos de datos que puede contener variables en Javascript son:

- **Numéricos (tipo `number`)**: puede contener cualquier tipo de número real (0.3, 1.7, 2.9) o entero (5, 3, -1).
- **Enteros grandes (tipo `bigint`)**: pueden contener enteros con valores superiores a 2^53 -1. Se pueden nomenclar escribiendo una letra “n” al final del entero. No pueden utilizarse con la mayoría de operadores matemáticos de Javascript.
- **Booleanos (tipo `boolean`)** : puede contener uno de los siguientes valores: `true`, `false`.
- **Cadenas (tipo `string`)**: cualquier combinación de caracteres (letras, números, signos especiales y espacios). Las cadenas se delimitan mediante comillas dobles `"` o simples `'` (“Lolo”,”lalO”). Para concatenar cadenas puede usarse el operador “+”.

```javascript
let edad = 23, nueva_edad, incremento=4;
const nombre = "Rosa García";
nueva_edad = edad + incremento;
console.log(nombre + " tras " + incremento + " años tendrá " + nueva_edad);
```

Asimismo existen otros tipos de datos en JavaScript: `undefined`, `null`, `symbol`, `object` y `function`. 

!!! info "Tipo `undefined`"
     Si una variable se declara pero no se inicializa, su valor será `undefined`

### 5.4.1 Conversiones entre tipos de datos

Javascript es un lenguaje débilmente tipado (es decir, al declarar una variable no se especifica un tipo), aunque internamente Javascript si maneje tipos de datos.

En determinados momentos, resulta necesario convertir un valor de un tipo a otro. Esto en JS se llama **coerción**, y puede ocurrir de forma **implícita** o podemos forzarlo de forma **explícita**.

- Forma **implícita**:
    + Convertir números a cadenas:
        ```javascript
        let numero = 5;
        console.log(numero);
        ```

- Forma **explícita**:
    + Convertir números a cadenas:
        ```javascript
        let numero = 5;
        console.log(numero.toString());
        ```
        ```javascript
        let cadena = "" + 3600;
        let longitudCadena = cadena.length; // longitud de la cadena (en este caso, 4)
        ```
    + Convertir cadenas a números:
        ```javascript
        let cadenaNumerica = "1234";
        let numero1 = parseInt(cadenaNumerica); // convierte la cadena a un número entero
        let numero2 = parseFloat(cadenaNumerica); // convierte la cadena a un número de punto flotante
        let numero3 = +"1234"; // convierte la cadena a un número utilizando el operador "+"
        ```

### 5.4.2 typeOf

El operador `typeof` devuelve una cadena que indica el tipo de dato de la expresión:
```javascript
let cadena = "abd";
let numero = 1234; 

console.log(typeof(cadenaNumerica));
console.log(typeof(numero));
```

## 5.5 Ejercicios

!!! question "Ejercicio 1 "
    Escribir un programa que calcule las siguientes operaciones.
    
    1.  (8+5)×3
    2.  20−(6+4)×2
    3.  (15−7)×(3+2)
    4.  50÷(5×2+5)
    5.  (12+8)÷(4+1)×3

    ??? tip "Solución"
        ```javascript

        // Mostrar el resultado
        console.log("(8+5)×3 =  " + XX);
        console.log("20−(6+4)×2 =  " + XX);
        console.log("(15−7)×(3+2) =  " + XX);
        console.log("50÷(5×2+5) =  " + XX);
        console.log("(12+8)÷(4+1)×3 =  " + XX);
        ```

!!! question "Ejercicio 2 "
    Repetir el ejercicio anterior pero eliminando los paréntesis de las operaciones, ¿el resultado es el mismo?.
    
    1.  8+5×3
    2.  20−6+4×2
    3.  15−7×3+2
    4.  50÷5×2+5
    5.  12+8÷4+1×3

    ??? tip "Solución"
        ```javascript

        // Mostrar el resultado
        console.log("8+5×3 =  " + XX);
        console.log("20−6+4×2 =  " + XX);
        console.log("15−7×3+2 =  " + XX);
        console.log("50÷5×2+5 =  " + XX);
        console.log("12+8÷4+1×3 =  " + XX);
        ```

!!! question "Ejercicio 3 "
    Escribir un programa que calcule el área de un cuadrado cuyo lado se introduce por teclado.
    
        Por ejemplo:
            Introduce el lado de un cuadrado: 2
            El área del cuadrado es: 4

    ??? tip "Solución"
        ```javascript
        // Pedir el lado del cuadrado
        let lado = parseFloat(prompt("Introduce el lado del cuadrado:"));

        // Calcular el área
        let area = lado * lado;

        // Mostrar el resultado
        console.log("El área del cuadrado es: " + area);
        ```

!!! question "Ejercicio 4"
    Escribir un programa que lea dos números decimales, calcule y muestre el valor de sus suma, resta, producto, división y resto en líneas distintas.
    
        Por ejemplo:
            Introduce un número: 2.5
            Introduce un número: 5.3
            La suma de 2.5 y 5.3 es: 7.8
            La resta de 2.5 y 5.3 es: -2.8
            El producto de 2.5 y 5.3 es: 13.25
            La división de 2.5 y 5.3 es: 0.47
            El resto de 2.5 y 5.3 es: 2.5

    ??? tip "Solución"
        ```javascript
        // Leer los números
        let num1 = parseFloat(prompt("Introduce el primer número:"));
        let num2 = parseFloat(prompt("Introduce el segundo número:"));

        // Operaciones
        let suma = num1 + num2;
        let resta = num1 - num2;
        let producto = num1 * num2;
        let division = num1 / num2;
        let resto = num1 % num2;

        // Mostrar resultados
        console.log("Suma: " + suma);
        console.log("Resta: " + resta);
        console.log("Producto: " + producto);
        console.log("División: " + division);
        console.log("Resto: " + resto);
        ```

!!! question "Ejercicio 5"
     Escribir un programa que solicite al usuario la base y la altura de un rectángulo, y muestre su área (base * altura) y perímetro (2 * base * altura).

     **Nota**: crear la constante PI, cuyo valor será de 3.14
    
        Por ejemplo:
            Introduce la base del rectángulo: 5
            Introduce la altura del rectángulo: 3,4
            Área del rectángulo es: 17
            Perímetro del rectángulo es: 34

    ??? tip "Solución"
        ```javascript
        // Leer base y altura
        let base = parseFloat(prompt("Introduce la base del rectángulo:"));
        let altura = parseFloat(prompt("Introduce la altura del rectángulo:"));

        // Calcular área y perímetro
        let area = base * altura;
        let perimetro = 2 * (base + altura);

        // Mostrar resultados
        console.log("Área del rectángulo: " + area);
        console.log("Perímetro del rectángulo: " + perimetro);
        ```

!!! question "Ejercicio 6"
     Escribir un programa que solicite una temperatura en grados Celsius y la convierta a Fahrenheit usando la fórmula: 
     **F=(C × 9/5) + 32**

        Por ejemplo:
            Introduce la la temperatura en grados Celsius: 25
            25 ºC equivalen a 77 ºF

    ??? tip "Solución"
        ```javascript
        // Leer temperatura en Celsius
        let celsius = parseFloat(prompt("Introduce la temperatura en grados Celsius:"));

        // Convertir a Fahrenheit
        let fahrenheit = (celsius * 9/5) + 32;

        // Mostrar resultado
        console.log(celsius + "°C equivalen a " + fahrenheit.toFixed(2) + "°F");
        ```

!!! question "Ejercicio 7"
     Escribir un programa que toma como dato de entrada un número que corresponde al radio de una circunferencia y muestre la longitud de la circunferencia (2 * PI * radio), el área del círculo (PI * radio * radio) y el volumen de la esfera ((4/3) * PI * radio * radio * radio) que corresponden con dicho radio.

     **Nota**: crear la constante PI, cuyo valor será de 3.14
    
        Por ejemplo:
            Introduce un el radio de una circunferencia: 5
            La longitud de la circunferencia es: XX
            El área del círculo es: XX
            El volumen de la esfera es: XX

    ??? tip "Solución"
        ```javascript
        // Crear constante PI
        const PI = 3.14;

        // Pedir el radio
        let radio = parseFloat(prompt("Introduce el radio de una circunferencia:"));

        // Cálculos
        let longitud = 2 * PI * radio;
        let area = PI * radio * radio;
        let volumen = (4/3) * PI * radio * radio * radio;

        // Mostrar resultados
        console.log("La longitud de la circunferencia es: " + longitud);
        console.log("El área del círculo es: " + area);
        console.log("El volumen de la esfera es: " + volumen);
        ```

!!! question "Ejercicio 8"
     Escribir un programa que dado el precio de un artículo y el precio de venta real, nos muestre el porcentaje de descuento realizado. La fórmula para calcular el procentaje de descuento es: ((precioArticulo - precioVenta) / precioArticulo) * 100;

     **Nota**: los precios pueden tener decimales.
    
        Por ejemplo:
            Introduce el precio del artículo: XX.X
            Introduce el precio real del artículo: XX.X
            El descuento realizado es: X%

    ??? tip "Solución"
        ```javascript
        let precioOriginal = parseFloat(prompt("Introduce el precio del artículo:"));
        let precioVenta = parseFloat(prompt("Introduce el precio real del artículo:"));

        // Calcular porcentaje de descuento
        let descuento = ((precioOriginal - precioVenta) / precioOriginal) * 100;

        // Mostrar resultado
        console.log("El descuento realizado es: " + descuento.toFixed(2) + "%");
        ```

!!! question "Ejercicio 9"
     Escribir un programa que solicite un número de dos cifras, y muestre por consola el número invertido. Para poder mostrar el número invertido, se deben obtener el número de decenas y unidades, y una vez obtenidas se mostrarán en orden inverso.

     Para obtener las decenas, se debe dividir el número introducido entre el valor 10; mientras que para obtener las unidades, se debe obtener el resto (módulo: %) del número introducido entre el valor 10.
     
        Por ejemplo:
            Dime un número de dos cifras: 23
            Primera cifra (decenas): 2
            Segunda cifra (unidades): 3
            El número invertido es 32

    ??? tip "Solución"
        ```javascript
        // Solicitar número de dos cifras
        let numero = parseInt(prompt("Dime un número de dos cifras:"));

        // Obtener decenas y unidades
        let decenas = Math.floor(numero / 10);
        let unidades = numero % 10;

        // Mostrar decenas y unidades
        console.log("Primera cifra (decenas): " + decenas);
        console.log("Segunda cifra (unidades): " + unidades);

        // Mostrar número invertido
        let invertido = (unidades * 10) + decenas;
        console.log("El número invertido es: " + invertido);
        ```

!!! question "Ejercicio 10"
    Escribir un programa que solicite tres números y muestre su media. Emplea variables para guardar los tres número solicitados y la media calculada. Dichas variables se deben declarar en una única línea
     
        Por ejemplo:
            Introduce el número 1: 3
            Introduce el número 2: 4,4
            Introduce el número 3: 7
            La media es: 4,8

    ??? tip "Solución"
        ```javascript
        // Declarar variables y leer los números en una línea
        let num1 = parseFloat(prompt("Introduce el número 1:")), 
            num2 = parseFloat(prompt("Introduce el número 2:")), 
            num3 = parseFloat(prompt("Introduce el número 3:")), 
            media;

        // Calcular la media
        media = (num1 + num2 + num3) / 3;

        // Mostrar resultado
        console.log("La media es: " + media.toFixed(1));
        ```

!!! question "Ejercicio 11"
    Escribir un programa que permita a los alumnos saber cual será su calificación en la asignatura de Programación.

    Para ello, la aplicación pedirá la introducción de tres notas parciales, una nota de examen y una nota de trabajo. A su vez, el programa mostrará la nota final teniendo en cuenta los siguientes porcentajes aplicables a cada una de las notas introducidas:

    1. 55% de promedio de sus tres calificaciones parciales. **Calculo Promedio**: (parcial1 + parcial2 + parcial3)/3
    2. 30% de la calificación del examen final.
    3. 15% de la calificación de un trabajo final.

    La fórmula para la **Nota Final** sería: (Promedio parciales × 0.55) + (Examen × 0.30) + (Trabajo × 0.15)
     
            Por ejemplo:
                Dime la nota del parcial 1: 4,5
                Dime la nota del parcial 2: 3,75
                Dime la nota del parcial 3: 5
                Dime la nota del examen: 5,5
                Dime la nota del trabajo: 6
                La nota final es: 4,98

    ??? tip "Solución"
        ```javascript
        // Pedir las notas
        let parcial1 = parseFloat(prompt("Dime la nota del parcial 1:"));
        let parcial2 = parseFloat(prompt("Dime la nota del parcial 2:"));
        let parcial3 = parseFloat(prompt("Dime la nota del parcial 3:"));
        let examen = parseFloat(prompt("Dime la nota del examen:"));
        let trabajo = parseFloat(prompt("Dime la nota del trabajo:"));

        // Calcular promedio de parciales
        let promedioParciales = (parcial1 + parcial2 + parcial3) / 3;

        // Calcular nota final
        let notaFinal = (promedioParciales * 0.55) + (examen * 0.30) + (trabajo * 0.15);

        // Mostrar resultado
        console.log("La nota final es: " + notaFinal.toFixed(2));
        ```

!!! question "Ejercicio 12"
    Escribir un programa que reciba un número de minutos y muestre cuántas horas y minutos representan. Las horas se calcularán dividiento los minutos entre 60, y los minutos restantes se calcularán obteniendo el resto de la división entre los minutos y 60.

        Por ejemplo:
            Introduce la cantidad en minutos: 130
            130 minutos son 2 horas y 10 minutos

    ??? tip "Solución"
        ```javascript
        // Leer cantidad de minutos
        let minutos = parseInt(prompt("Introduce la cantidad de minutos:"));

        // Calcular horas y minutos restantes
        let horas = parseInt(minutos / 60);
        let minutosRestantes = minutos % 60;

        // Mostrar resultado
        console.log(minutos + " minutos son " + horas + " horas y " + minutosRestantes + " minutos");
        ```

## 5.6 Ejercicios Extra

!!! question "Ejercicio 1"
    Escribir un programa que reciba por teclado el precio de un producto y un procentaje de descuento. El programa debe mostrar en la **consola** el precio del producto aplicando el descuento. La fórmula para obtener el precio del producto con el descuento es: **precioProducto - (precioProducto * descuento) / 100**

        Por ejemplo:
            Introduce precio de un producto: 130
            Introduce descuento del producto: 20
            El precio del producto aplicando el descuento es: 104

    ??? tip "Solución"
        ```javascript
        // Pedir datos
        let precio = parseFloat(prompt("Introduce el precio de un producto:"));
        let descuento = parseFloat(prompt("Introduce el decuento del producto:"));

        // Calcular descuento y precio final
        let precioFinal = precio - (precio * descuento) / 100;

        // Mostrar resultado
        console.log("El precio del producto aplicando el descuento es: " + precioFinal);
        ```

!!! question "Ejercicio 2"
    Escribir un programa que reciba por teclado el precio de un producto y calcule el precio final aplicando el 21% de IVA. El programa debe mostrar el iva calculado y el precio final con el iva aplicado. El IVA se calcula con la siguiente ecuación: **precio * 21 / 100**, y el precio final se calcula con la siguiente ecuación: **precio + IVA**

        Por ejemplo:
            Introduce precio de un producto: 130
            El IVA es de: 27,7 euros
            EL precio con el IVA es: 157,7 euros

    ??? tip "Solución"
        ```javascript
        // Pedir precio
        let precio = parseFloat(prompt("Introduce el precio del producto:"));

        // Calcular IVA y total
        let iva = precio * 0.21;
        let total = precio + iva;

        // Mostrar resultado
        console.log("El IVA es de: " + iva + " euros");
        console.log("Precio con IVA es: " + total + " euros");
        ```