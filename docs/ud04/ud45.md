# 5. Modificar nodos en el DOM
Una vez que hemos encontrado los nodos, podemos modificarlos. Algunos métodos útiles incluyen:

- `.innerHTML`, `.innerText`, `.outerHTML`: Permite modificar el contenido HTML o texto de un elemento.
- `.insertAdjacentHTML(position, text)`: Permite inserta texto HTML en una posición específica.
- `.append(content, element)`, `.prepend(content, element)`: Permite añadir contenido al principio o al final de un elemento.
- `.after()`, `.before()`: Permite insertar un elemento antes o después del elemento actual.
- `.cloneNode(deep)`: Permite clonar un nodo, con o sin sus hijos.
- `.remove()`: Permite eliminar un nodo.

Métodos más antiguos pero aún en uso incluyen `removeChild()` y `appendChild()`.

Más elemento en: [Elementos del DOM](https://www.w3schools.com/jsref/dom_obj_all.asp)

## 5.1 Métodos

### 5.1.1 innerHTML, innerText, outerHTML

??? example "Ejemplo 1 - innerHTML: Obtener el contenido HTML de un elemento con id="myP""
                
    === "index.html"

        ```html
        <!DOCTYPE html>
        <html lang="es">
            <head>
                <meta charset="UTF-8">
                <script src="app.js" defer></script>
            </head>
            <body>
                <h1>Element Object Model</h1>
                <h2>Propiedad innerHTML</h2>

                <p id="myP"><b>Soy un párrafo</b></p>

                <p>El contenido de "myP" es:</p>
                <div id="demo"></div>

                <button onclick="modificarHTML()">Añadir párrafo</button>
            </body>
        </html>
        ```
    === "app.js"

        ```javascript
        function modificarHTML(){
            let html = document.querySelector("#myP").innerHTML; 
            
            document.querySelector("#demo").innerHTML = html; //(1)
        }
        ```

        1. Interpreta etiquetas HTML

??? example "Ejemplo 2 - innerText: Obtener el texto interno de un elemento"
                
    === "index.html"

        ```html
        <!DOCTYPE html>
        <html lang="es">
            <head>
                <meta charset="UTF-8">
                <script src="app.js" defer></script>
            </head>
            <body>
                <h1>Element Object Model</h1>
                <h2>Propiedad innerText</h2>

                <p id="myP"><b>Soy un párrafo</b></p>

                <p>El contenido de "myP" es:</p>
                <div id="demo"></div>

                <button onclick="modificarHTML()">Añadir párrafo</button>
            </body>
        </html>
        ```
    === "app.js"

        ```javascript
        function modificarHTML(){
            let html = document.querySelector("#myP").innerHTML; 
            
            document.querySelector("#demo").innerText = html; //(1)
        }
        ```

        1. Ignora etiquetas HTML (las trata como texto)

??? example "Ejemplo 3 - outerHTML: Modifica todo el elemento incluyendo la propia etiqueta"
                
    === "index.html"

        ```html
        <!DOCTYPE html>
        <html lang="es">
            <head>
                <meta charset="UTF-8">
                <script src="app.js" defer></script>
            </head>
            <body>
                <h1>Element Object Model</h1>
                <h2>Propiedad innerText</h2>

                <p id="myP">Párrafo</p>

                <button onclick="modificarHTML()">Añadir párrafo</button>
            </body>
        </html>
        ```
    === "app.js"

        ```javascript
        function modificarHTML(){
            let elemento = document.getElementById("myP");

            elemento.outerHTML =  "<h2>Esto es un elemento H2</h2>";
        }
        ```

### 5.1.2 insertAdjacentHTML

Método que permite insertar código HTML en una posición específica. Las posición donde se puede insertar código son:

- `afterbegin`: Después del inicio del elemento (primer hijo).
- `afterend`: Después del elemento.
- `beforebegin`: Antes del elemento.
- `beforeend`: Antes del final del elemento (último hijo).

??? example "Ejemplo 1 - Insertar un nuevo elemento `<p>` después de un elemento"
                
    === "index.html"

        ```html
        <!DOCTYPE html>
        <html lang="es">
            <head>
                <meta charset="UTF-8">
                <script src="app.js" defer></script>
            </head>
            <body>
                <h1>Element Object Model</h1>
                <h2>Método insertAdjacentHTML</h2>

                <button onclick="myFunction()">Insertar</button>
                <p>Haz clic en "Insertar" para insertar un párrafo después del encabezado.</p>

                <h2 id="myH2">Mi encabezado</h2>
            </body>
        </html>
        ```
    === "app.js"

        ```javascript
        function myFunction() {
            let h2 = document.getElementById("myH2");
            let html = "<p>My new paragraph.</p>";

            h2.insertAdjacentHTML("afterend", html);
        }
        ```

### 5.1.3 append, prepend

#### 5.1.3.1 append

El método `append` se utiliza para agregar uno o varios nodos (elementos) o cadenas después del último hijo de un elemento.

??? example "Ejemplo 1 - Agregar un elemento después de un div"
                
    === "index.html"

        ```html
        <!DOCTYPE html>
        <html lang="es">
            <head>
                <meta charset="UTF-8">
                <script src="app.js" defer></script>
            </head>
            <body>
                <h1>Element Object Model</h1>
                <h2>Método append</h2>

                <div id="myDiv"><h3>Primer elemento</h3></div>

                <p>Haz clic en "Añadir" para agregar elementoa un div:</p>

                <button onclick="myFunction()">Añadir</button>
            </body>
        </html>
        ```
    === "app.js"

        ```javascript
        function myFunction() {
            let div = document.querySelector("#myDiv");
            let nuevoParrafo = document.createElement('p');

            nuevoParrafo.textContent = 'Segundo párrafo';

            div.append(nuevoParrafo);
        }
        ```

#### 5.1.3.2 prepend

El método `prepend` se utiliza para agregar uno o varios nodos (elementos) o cadenas antes del primer hijo de un elemento.

??? example "Ejemplo 2 - Agregar un elemento antes de un div"
                
    === "index.html"

        ```html
        <!DOCTYPE html>
        <html lang="es">
            <head>
                <meta charset="UTF-8">
                <script src="app.js" defer></script>
            </head>
            <body>
                <h1>Element Object Model</h1>
                <h2>Método prepend</h2>

                <div id="myDiv"><h3>Segundo elemento</h3></div>

                <p>Haz clic en "Añadir" para agregar elementoa un div:</p>

                <button onclick="myFunction()">Añadir</button>
            </body>
        </html>
        ```
    === "app.js"

        ```javascript
        function myFunction() {
            let div = document.querySelector("#myDiv");
            let nuevoParrafo = document.createElement('p');

            nuevoParrafo.textContent = 'Primer párrafo';

            div.prepend(nuevoParrafo);
        }
        ```

### 5.1.4 after, before

#### 5.1.4.1 after

El método `after` es usado para insertar uno o más nodos (elementos) o cadenas de texto (string) inmediatamente después de un elemento.

??? example "Ejemplo 1 - Agregar un elemento después de un di"
                
    === "index.html"

        ```html
        <!DOCTYPE html>
        <html lang="es">
            <head>
                <meta charset="UTF-8">
                <script src="app.js" defer></script>
            </head>
            <body>
                <h1>Element Object Model</h1>
                <h2>Método after</h2>

                <p id="miP">Mi párrado</p>

                <div id="myDiv">Hola</div>

                <p>Haz clic en "Añadir" para agregar un elemento después de un div:</p>

                <button onclick="myFunction()">Click aquí</button>
            </body>
        </html>
        ```
    === "app.js"

        ```javascript
        function myFunction() {
            let div = document.querySelector("#myDiv");
            let nuevoParrafo = document.createElement('p');

            nuevoParrafo.textContent = 'Contenido añadido';

            div.after(nuevoParrafo);
        }
        ```

#### 5.1.4.2 before

El método `before` es usado para insertar uno o más nodos (elementos) o cadenas de texto (string) inmediatamente antes de un elemento.

??? example "Ejemplo 2 - Agregar un elemento antes de un div"
                
    === "index.html"

        ```html
        <!DOCTYPE html>
        <html lang="es">
            <head>
                <meta charset="UTF-8">
                <script src="app.js" defer></script>
            </head>
            <body>
                <h1>Element Object Model</h1>
                <h2>Método before</h2>

                <p id="miP">Mi párrado</p>

                <div id="myDiv">Hola</div>

                <p>Haz clic en "Añadir" para agregar un elemento antes de un div:</p>

                <button onclick="myFunction()">Click aquí</button>
            </body>
        </html>
        ```
    === "app.js"

        ```javascript
        function myFunction() {
            let div = document.querySelector("#myDiv");
            let nuevoParrafo = document.createElement('p');

            nuevoParrafo.textContent = 'Contenido añadido';

            div.before(nuevoParrafo);
        }
        ```

### 5.1.5 cloneNode

El método `cloneNode(deep)` crea una copia de un nodo y devuelve el clon.

??? example "Ejemplo 1 - Copia un elemento `<li>` desde myList2 a myList1"
                
    === "index.html"

        ```html
        <!DOCTYPE html>
        <html lang="es">
            <head>
                <meta charset="UTF-8">
                <script src="app.js" defer></script>
            </head>
            <body>
                <h1>Element Object Model</h1>
                <h2>Método cloneNode</h2>

                <button onclick="myFunction()">Copiar</button>
                <p>Haz clic en "Copiar" para copiar un elemento de una lista a otra.</p>

                <ul id="myList1">
                    <li>Coffee</li>
                    <li>Tea</li>
                </ul>
                <ul id="myList2">
                    <li>Water</li>
                    <li>Leche</li>
                </ul>

                <p>Si cambia el parámetro <b>deep</b> a false, solo se clonará un elemento LI vacío.</p>
            </body>
        </html>
        ```
    === "app.js"

        ```javascript
        function myFunction() {
            let node = document.querySelector("#myList2").lastElementChild;
            let clone = node.cloneNode(true);

            document.querySelector("#myList1").appendChild(clone);
        }
        ```

### 5.1.6 remove

El método `remove` elimina un elemento o nodo del document.

??? example "Ejemplo 1 - Eliminar un elemento del document"
                
    === "index.html"

        ```html
        <!DOCTYPE html>
        <html lang="es">
            <head>
                <meta charset="UTF-8">
                <script src="app.js" defer></script>
            </head>
            <body>
                <h1>Element Object Model</h1>
                <h2>Método remove</h2>

                <p id="demo">Haz clic en "Eliminar" y este párrafo se eliminará del DOM..</p>
                <button onclick="myFunction()">Eliminar</button>
            </body>
        </html>
        ```
    === "app.js"

        ```javascript
        function myFunction() {
                let element = document.querySelector("#demo");

                element.remove();
        }
        ```

## 5.2 Atributos

Los elementos suelen tener **atributos**. Algunos son especiales como `id` o `class`. El **id** está accesible directamente como atributo del elemento, así como el **className**, aunque luego veremos que es mejor manipularlo de otra manera. Otros atributos como **value** en los `Input` o **src** en los `<img>` también pueden ser leídos y modificados de forma directa.

Sin embargo, para los atributos que no tienen acceso directo porque no son estándar, podemos usar `setAttribute()`, `getAttribute()`, `hasAttribute()` o `removeAttribute()`:

??? example "Ejemplo 1 - Acceso a los atributos de los elementos"
                
    === "index.html"

        ```html
        <!DOCTYPE html>
        <html lang="es">
            <head>
                <meta charset="UTF-8">
                <script src="app.js" defer></script>
            </head>
            <body>
                <h1>Element Object Model</h1>

                <button onclick="myFunction()">Botón</button>
                <p id="myP"></p>
            </body>
        </html>
        ```
    === "app.js"

        ```javascript
        function myFunction() {
            let button = document.querySelector("button");
            
            button.setAttribute("name", "helloButton");
            button.disabled = true;

            document.querySelector("#myP").textContent = button.getAttribute("name");
        }
        ```

## 5.3 Estilos

Para manipular los estilos de un elemento, podemos usar propiedades de estilo y clases CSS:

- `.style.property`: Modifica un estilo CSS directamente.
- `.className`: Cambia el nombre de la clase del elemento.
- `.classList.add()`, `.classList.toggle()`, `.classList.remove()`, `.classList.replace()`: Métodos para manipular clases CSS de manera más dinámica.

??? example "Ejemplo 1 - Acceso a los atributos de los elementos"
                
    === "index.html"

        ```html
        <!DOCTYPE html>
        <html lang="es">
            <head>
                <meta charset="UTF-8">
                <script src="app.js" defer></script>
                <link rel="stylesheet" href="estilos.css">
            </head>
            <body>
                <h1>Element Object Model</h1>

                <div id="myId">Manipulación de estilos CSS</div>

                <button onclick="anyadirColor()">Añadir Color</button>
                <button onclick="asignarCSS()">Asignar CSS</button>
                <button onclick="borrarCSS()">Eliminar CSS</button>
                <button onclick="anyadirCSS()">Añadir CSS</button>
            </body>
        </html>
        ```
    === "app.js"

        ```javascript
        function anyadirColor() {
            let element = document.querySelector('#myId');

            element.style.color = 'red';
        }

        function asignarCSS() {
            let element = document.querySelector('#myId');

            element.style.color = '';
            element.className = 'normal';
        }

        function borrarCSS() {
            let element = document.querySelector('#myId');

            element.style.color = '';
            element.classList.remove('normal');
        }

        function anyadirCSS() {
            let element = document.querySelector('#myId');

            element.style.color = '';
            element.classList.add('tamanyo');
        }
        ```

    === "estilos.css"

        ```css
        .normal {
            text-decoration: line-through;
            color: gray;
        }

        .importante {
            color: blue;
            font-weight: bold;
        }

        .tamanyo {
            font-size: 20px;
        }
        ```

## 5.4 Ejercicios

### 5.4.1 Atributos

!!! note "Ejercicios 'Atributo'"

    ??? example "Ejercicio 1: Cambiar foto de perfil. Al pulsar sobre el botón 'Cambiar perfil', se debe cambiar la imagen 'gato_1' por otra imagen. Además, el nombre 'Juan' se debe cambiar por el nombre 'Maria'. (Las imágenes deben estar guardadas en el mismo directorio que los fichero index.html y app.js)"
                
        === "index.html"

            ```html
            <!DOCTYPE html>
            <html lang="es">
                <head>
                    <meta charset="UTF-8">
                    <script src="app.js" defer></script>
                    <title>Perfil</title>
                </head>
                <body>
                    <h2>Mi perfil</h2>

                    <img id="foto" src="gato_1.jpg" alt="foto" width="50%" height="50%">

                    <p id="nombre">Nombre: Juan</p>

                    <button onclick="cambiarPerfil()">Cambiar perfil</button>
                </body>
            </html>
            ```
        === "app.js"

            ```javascript
            function cambiarPerfil() {
                let foto;

                //1. Obtener el elemento <img id="foto" src="gato_1.jpg" alt="foto" width="50%" height="50%"> y almacenarlo en la variable 'foto'.

                //2. Cambiar el atibuto src de la variable 'foto' por otra imagen.

                //3. Si la imagen cargada en la variable 'foto' es igual a foto_1 se deberá cargar la foto_2, sino, se cargará la foto_1.
            }
            ```

        === "SOLUCIÓN"

            ```javascript
            function cambiarPerfil() {
                let foto;

                //1. Obtener el elemento <img id="foto" src="gato_1.jpg" alt="foto" width="50%" height="50%"> y almacenarlo en la variable 'foto'.
                foto = document.getElementById("nombre").textContent = "Nombre: María";

                //2. Cambiar el atibuto src de la variable 'foto' por otra imagen.
                foto.src = "gato_2.jpg";

                //3. Si la imagen cargada en la variable 'foto' es igual a foto_1 se deberá cargar la foto_2, sino, se cargará la foto_1.
                foto = document.querySelector("#foto");

                if(foto.src === "file:///C:/Users/Trino/Desktop/ejemplo/gato_1.jpg"){
                    foto.src = "gato_2.jpg";
                }
                else{
                    foto.src = "gato_1.jpg";
                }
            }
            ```

    ??? example "Ejercicio 2: Activar oferta en producto. Al pulsasr sobre el botón 'Aplicar Oferta', se debe cambiar la dirección web por otra dirección de internet que contenga una televisión en oferta, además, el texto de la imagen debe cambiar de 'Ver producto' a 'Ver Oferta' y el precio debe mostrar el siguiente mensaje: 'Precio: 400 € (OFERTA)'"
                
        === "index.html"

            ```html
            <!DOCTYPE html>
            <html lang="es">
                <head>
                    <meta charset="UTF-8">
                    <script src="app.js" defer></script>
                    <title>Oferta</title>
                </head>
                <body>
                    <h2>Producto</h2>

                    <p id="producto">Televisor</p>
                    <p id="precio">Precio: 500 €</p>

                    <a id="enlace" href="https://www.mediamarkt.es/es/product/_tv-oled-65-samsung-tq65s95fatxxc-oled-4k-nq4-ai-gen3-processor-smart-tv-full-ai-wifi-negro-grafito-1598356.html">Ver producto</a>

                    <br><br>

                    <button onclick="aplicarOferta()">Aplicar oferta</button>
                </body>
            </html>
            ```
        === "app.js"

            ```javascript
            function aplicarOferta() {
                // 1. Cambiar del elemento con id="precio" el texto por el siguiente: "Precio: 400 € (OFERTA)"

                // 2. Cambiar del elemento con id="enlace" el atributo href por la siguiente URL: https://www.informacion.es

                // 3. Cambiar del elemento con id="enlace" el texto por el siguiente: "Ver oferta"
            }
            ```

        === "SOLUCIÓN"

            ```javascript
            function aplicarOferta() {
                // 1. Cambiar del elemento con id="precio" el texto por el siguiente: "Precio: 400 € (OFERTA)"
                document.getElementById("precio").textContent = "Precio: 400 € (OFERTA)";

                // 2. Cambiar del elemento con id="enlace" el atributo href por la siguiente URL: https://www.informacion.es
                document.getElementById("enlace").href = "https://example.com/oferta";

                // 3. Cambiar del elemento con id="enlace" el texto por el siguiente: "Ver oferta"
                document.getElementById("enlace").textContent = "Ver oferta";
            }
            ```

### 5.4.2 append y prepend

!!! note "Ejercicios con `append` y `prepend`"

    ??? example "Ejercicio 3: Crear una lista de la compra donde se añadan productos a una lista."
                
        === "index.html"

            ```html
            <!DOCTYPE html>
            <html lang="es">
                <head>
                    <meta charset="UTF-8">
                    <script src="app.js" defer></script>
                    <title>Lista de la compra</title>
                </head>
                <body>
                    <h2>Lista de la compra</h2>

                    <input type="text" id="producto" placeholder="Producto">
                    <button onclick="anyadir()">Añadir</button>

                    <ul id="lista"></ul>
                </body>
            </html>
            ```
        === "app.js"

            ```javascript
            function anyadir() {
                let texto, lista, li;

                // 1. Obtener el valor del input (value) del elemento <input type="text" id="producto" placeholder="Producto">. Almacenar el valor recuperado en la varibale 'texto'.

                // 2. Obtener el elemento <ul id="lista"></ul> y almacenarlo en la variable 'lista'.

                // 3. Crear un elemento de tipo <li> y asignarlo a la variable 'li'

                // 4. Asignar al elemento 'li' creado en el punto 3 el texto obtenido en el punto 1.

                // 5. Añadir el elemento creado en el punto 3 al elemento obtenido en el punto 2. Emplear 'append'
            }
            ```

        === "SOLUCIÓN"

            ```javascript
            function anyadir() {
                let texto, lista, li;

                // 1. Obtener el valor del input que tenga como id = producto. Almacenar el texto recuperado en la varibale texto.
                texto = document.querySelector("#producto").value;

                // 2. Obtener el elemento <ul id="lista"></ul> y almacenarlo en la variable 'lista'.
                lista = document.querySelector("#lista");

                // 3. Crear un elemento de tipo <li> y asignarlo a la variable 'li'
                li = document.createElement("li");

                // 4. Asignar al elemento 'li' creado en el punto 3 el texto obtenido en el punto 1.
                li.textContent = texto;

                // 5. Añadir el elemento creado en el punto 3 al elemento obtenido en el punto 2. Emplear 'append'
                lista.append(li);
            }
            ```

    ??? example "Ejercicio 4: Crear una lista con tareas urgentes"
                
        === "index.html"

            ```html
            <!DOCTYPE html>
            <html lang="es">
                <head>
                    <meta charset="UTF-8">
                    <script src="app.js" defer></script>
                    <title>Tareas</title>
                </head>
                <body>
                    <h2>Tareas</h2>

                    <input type="text" id="tarea" placeholder="Tarea">
                    
                    <select id="tipo">
                        <option value="normal">Normal</option>
                        <option value="urgente">Urgente</option>
                    </select>

                    <button onclick="crear()">Añadir</button>

                    <ul id="agenda"></ul>
                </body>
            </html>
            ```
        === "app.js"

            ```javascript
            function crear() {
                let texto, tipo, li;

                // 1. Obtener el valor (value) del elemento <input type="text" id="tarea" placeholder="Tarea">. Almacenar el valor recuperado en la varibale texto.

                // 2. Obtener el valor seleccionado del elemento <select id="tipo">. Almacenar el texto recuperado en la varibale tipo.
                tipo = document.querySelector("#tipo").value;

                // 3. Crear un elemento de tipo <li> y asignarlo a la variable 'li'

                // 4. Asignar al elemento 'li' creado en el punto 3 el texto obtenido en el punto 1.

                // 5. Si tipo es igual a 'urgente', añadir un color a la variable li (li.style.color = "red";)

                // 6. Si tipo es igual a 'urgente', añadir el elemento <li> creado en el punto 2 al principio del elemento <ul id="agenda"></ul>, sino,
                // el elemento <li> se debe añadir al final del elemento <ul id="agenda"></ul>. Utilizar append y prepend.

                // 7. Borrar el texto escrito en el input con id = "producto"    
            }
            ```

        === "SOLUCIÓN"

            ```javascript
            function crear() {
                let texto, tipo, li;

                // 1. Obtener el valor (value) del elemento <input type="text" id="tarea" placeholder="Tarea">. Almacenar el valor recuperado en la varibale texto.
                texto = document.querySelector("#tarea").value;

                // 2. Obtener el valor seleccionado del elemento <select id="tipo">. Almacenar el texto recuperado en la varibale tipo.
                tipo = document.querySelector("#tipo").value;

                // 3. Crear un elemento de tipo <li> y asignarlo a la variable 'li'
                li = document.createElement("li");

                // 4. Asignar al elemento 'li' creado en el punto 3 el texto obtenido en el punto 1.
                li.textContent = texto;

                // 5. Si tipo es igual a 'urgente', añadir un color a la variable li (li.style.color = "red";)
                if (tipo === "urgente") {
                    li.style.color = "red";
                }

                // 6. Si tipo es igual a 'urgente', añadir el elemento <li> creado en el punto 2 al principio del elemento <ul id="agenda"></ul>, sino,
                // el elemento <li> se debe añadir al final del elemento <ul id="agenda"></ul>. Utilizar append y prepend.
                if (tipo === "urgente") {
                    document.querySelector("#agenda").prepend(li);
                } else {
                    document.querySelector("#agenda").append(li);
                }

                // 7. Borrar el texto escrito en el input con id = "producto"    
                document.querySelector("#tarea").value = "";
            }
            ```

### 5.4.3 before y after

!!! note "Ejercicios con `before` y `after`"

    ??? example "Ejercicio 5: Insertar avisos"
                
        === "index.html"

            ```html
            <!DOCTYPE html>
            <html lang="es">
                <head>
                    <meta charset="UTF-8">
                    <title>Pedido</title>
                    <script src="app.js" defer></script>
                </head>
                <body>
                    <h2>Pedido</h2>

                    <p id="producto">Producto: Televisor</p>

                    <br>

                    <button onclick="antes()">Añadir aviso antes</button>
                    <button onclick="despues()">Añadir aviso después</button>
                </body>
            </html>
            ```
        === "app.js"

            ```javascript
            function antes() {
                let aviso;

                // 1. Crear un elemento de tipo párrafo (p) y almacenarlo en la variable aviso.

                // 2. Asignar a la variable 'aviso' el texto "⚠ Oferta limitada" y el color rojo.

                // 3. Añadir antes del elemento <p id="producto">Producto: Televisor</p> el elemento creado en el punto 1
            }

            function despues() {
                let info;
                
                // 1. Crear un elemento de tipo párrafo (p) y almacenarlo en la variable info.

                // 2. Asignar a la variable 'info' el texto "Entrega en 24 horas" y el color verde.

                // 3. Añadir después del elemento <p id="producto">Producto: Televisor</p> el elemento creado en el punto 1
            }
            ```

        === "SOLUCIÓN"

            ```javascript
            function antes() {
                let aviso;

                // 1. Crear un elemento de tipo párrafo (p) y almacenarlo en la variable aviso.
                aviso = document.createElement("p");

                // 2. Asignar a la variable 'aviso' el texto "⚠ Oferta limitada" y el color rojo.
                aviso.textContent = "⚠ Oferta limitada";
                aviso.style.color = "red";

                // 3. Añadir antes del elemento <p id="producto">Producto: Televisor</p> el elemento creado en el punto 1
                document.querySelector("#producto").before(aviso);
            }

            function despues() {
                let info;
                
                // 1. Crear un elemento de tipo párrafo (p) y almacenarlo en la variable info.
                info= document.createElement("p");

                // 2. Asignar a la variable 'info' el texto "Entrega en 24 horas" y el color verde.
                info.textContent = "Entrega en 24 horas";
                info.style.color = "green";

                // 3. Añadir después del elemento <p id="producto">Producto: Televisor</p> el elemento creado en el punto 1
                document.querySelector("#producto").after(info);
            }
            ```

    ??? example "Ejercicio 6: Agenda con pausas automáticas"
                
        === "index.html"

            ```html
            <!DOCTYPE html>
            <html lang="es">
                <head>
                    <meta charset="UTF-8">
                    <script src="app.js" defer></script>
                    <title>Agenda</title>
                </head>
                <body>
                    <h2>Agenda del día</h2>

                    <ul>
                        <li id="trabajo">Trabajo (2h)</li>
                    </ul>

                    <br>

                    <button onclick="anyadirPausa()">Añadir pausa</button>
                    <button onclick="anyadirPreparacion()">Añadir preparación</button>
                </body>
            </html>
            ```
        === "app.js"

            ```javascript
            function anyadirPausa() {
                let pausa;
                
                // 1. Crear un elemento de tipo lista (li) y almacenarlo en la variable pausa.

                // 2. Asignar a la variable 'pausa' el texto "Descanso (10 min)" y el color de fondo (backgroundColor) lightblue.

                // 3. Añadir antes del elemento <li id="trabajo">Trabajo (2h)</li> el elemento creado en el punto 1
            }

            function anyadirPreparacion() {
                let prep;
                
                // 1. Crear un elemento de tipo lista (li) y almacenarlo en la variable prep.

                // 2. Asignar a la variable 'prep' el texto "Preparación previa" y el efecto negrita (fontWeight = "bold").

                // 3. Añadir después del elemento <li id="trabajo">Trabajo (2h)</li> el elemento creado en el punto 1
            }
            ```

        === "SOLUCIÓN"

            ```javascript
            function anyadirPausa() {
                let pausa;
                
                // 1. Crear un elemento de tipo lista (li) y almacenarlo en la variable pausa.
                pausa = document.createElement("li");

                // 2. Asignar a la variable 'pausa' el texto "Descanso (10 min)" y el color de fondo (backgroundColor) lightblue.
                pausa.textContent = "Descanso (10 min)";
                pausa.style.backgroundColor = "lightblue";

                // 3. Añadir antes del elemento <li id="trabajo">Trabajo (2h)</li> el elemento creado en el punto 1
                document.querySelector("#trabajo").after(pausa);
            }

            function anyadirPreparacion() {
                let prep;
                
                // 1. Crear un elemento de tipo lista (li) y almacenarlo en la variable prep.
                prep = document.createElement("li");

                // 2. Asignar a la variable 'prep' el texto "Preparación previa" y el efecto negrita (fontWeight = "bold").
                prep.textContent = "Preparación previa";
                prep.style.fontWeight = "bold";

                // 3. Añadir después del elemento <li id="trabajo">Trabajo (2h)</li> el elemento creado en el punto 1
                document.querySelector("#trabajo").before(prep);
            }
            ```

### 5.4.4 remove

!!! note "Ejercicios con `remove`"

    ??? example "Ejercicio 7: Eliminar productos caros"
                
        === "index.html"

            ```html
            <!DOCTYPE html>
            <html lang="es">
                <head>
                    <meta charset="UTF-8">
                    <script src="app.js" defer></script>
                    <title>Eliminar caros</title>
                </head>
                <body>
                    <h2>Productos</h2>

                    <ul id="lista">
                        <li value="2">Pan - 2€</li>
                        <li value="25">Jamón - 25€</li>
                        <li value="3">Leche - 3€</li>
                    </ul>

                    <button onclick="eliminar()">Eliminar caros</button>
                </body>
            </html>
            ```
        === "app.js"

            ```javascript
            function eliminar() {
                let i = 0;
                let precio, items;

                // 1. obtener los elementos hijos del elemento <ul id="lista">, los cuales son de tipo <li> y guardarlos en la variable 'items'.

                // 2. Recorrer todos los elementos de la variable items. Para cada uno de ellos, obtener el precio (value) y almacenarlo en la variable 'precio'.
                    // Si el precio es mayor a 20 euros, se eliminará el elemento <li> de la lista de items.
                
            }
            ```

        === "SOLUCIÓN"

            ```javascript
            function eliminar() {
                let i = 0;
                let precio, items;

                // 1. obtener los elementos hijos del elemento <ul id="lista">, los cuales son de tipo <li> y guardarlos en la variable 'items'.
                items = document.querySelector("#lista").children;

                // 2. Recorrer todos los elementos de la variable items>. Para cada uno de ellos, obtener el precio.
                    // Si el precio es mayor a 20 euros, se eliminará el elemento <li> de la lista de items.
                while(i < items.length){
                    precio = items[i].value;

                    if (precio > 20) {
                        items[i].remove();
                    }

                    i++;
                }
            }
            ```

    ??? example "Ejercicio 8: Lista de tareas con borrado"
                
        === "index.html"

            ```html
            <!DOCTYPE html>
            <html lang="es">
                <head>
                    <meta charset="UTF-8">
                    <script src="app.js" defer></script>
                    <title>Lista de tareas</title>
                    <style>
                        li {
                            margin: 5px;
                        }
                        button {
                            margin-left: 10px;
                        }
                    </style>
                </head>
                <body>
                    <h2>Lista de tareas</h2>

                    <input type="text" id="tareaNombre" placeholder="Nueva tarea">
                    <input type="text" id="tareaId" placeholder="Id tarea">

                    <button onclick="anyadir()">Añadir</button>
                    <button onclick="borrar()">Borrar</button>

                    <ul id="lista"></ul>
                </body>
            </html>
            ```
        === "app.js"

            ```javascript
            function anyadir() {
                let texto, id;
                let li;
                
                // 1. Obtener el valor (value) del elemento <input type="text" id="tareaNombre" placeholder="Nueva tarea"> y almacenarlo en la variable 'texto'.

                // 2. Obtener el valor (value) del elemento <input type="text" id="tareaId" placeholder="Id tarea"> y almacenarlo en la variable 'id'.

                // 3. Crear un elemento de tipo 'li' y almacenarlo en la variable 'li'

                // 4. Asignar al elemento li el texto (textoContent) almacenado en la variable 'texto' y el id (id) almacenado en la variable 'id'.

                // 5. Añadir el elemento li creado en el punto 3 al elemento <ul id="lista"></ul>. Emplear el método append

                // 6. Borrar los texto escritos en los inputs
            }

            function borrar() {
                let id;
                
                // 1. Obtener el valor (value) del elemento <input type="text" id="tareaId" placeholder="Id tarea"> y almacenarlo en la variable 'id'.

                // 2. Obtener el elemento que tenga el id obtenido en el punto1. Almacenarlo en la variable 'elemento'

                // 3. Eliminar la variable elemento.

                // 4. Borrar los texto escritos en los inputs
            }
            ```

        === "SOLUCIÓN"

            ```javascript
            function anyadir() {
                let texto, id;
                let li;
                
                // 1. Obtener el valor (value) del elemento <input type="text" id="tareaNombre" placeholder="Nueva tarea"> y almacenarlo en la variable 'texto'.
                texto = document.querySelector("#tareaNombre").value;

                // 2. Obtener el valor (value) del elemento <input type="text" id="tareaId" placeholder="Id tarea"> y almacenarlo en la variable 'id'.
                id = document.querySelector("#tareaId").value;

                // 3. Crear un elemento de tipo 'li' y almacenarlo en la variable 'li'
                li = document.createElement("li");

                // 4. Asignar al elemento li el texto (textoContent) almacenado en la variable 'texto' y el id (id) almacenado en la variable 'id'.
                li.textContent = texto;
                li.id = id;

                // 5. Añadir el elemento li creado en el punto 3 al elemento <ul id="lista"></ul>. Emplear el método append
                document.querySelector("#lista").append(li);

                // 6. Borrar los texto escritos en los inputs
                document.querySelector("#tareaNombre").value = "";
                document.querySelector("#tareaId").value = "";
            }

            function borrar() {
                let id;
                
                // 1. Obtener el valor (value) del elemento <input type="text" id="tareaId" placeholder="Id tarea"> y almacenarlo en la variable 'id'.
                id = document.querySelector("#tareaId").value;

                // 2. Obtener el elemento que tenga el id obtenido en el punto1. Almacenarlo en la variable 'elemento'
                elemento = document.getElementById(id);

                // 3. Eliminar la variable elemento.
                elemento.remove();

                // 4. Borrar los texto escritos en los inputs
                document.querySelector("#tareaId").value = "";
            }
            ```

### 5.4.5 Estilos

!!! note "Ejercicios con `estilos`"

    ??? example "Ejercicio 9: Aplicar estilos con clases CSS"
                
        === "index.html"

            ```html
            <!DOCTYPE html>
            <html lang="es">
                <head>
                    <meta charset="UTF-8">
                    <script src="app.js" defer></script>
                    <link rel="stylesheet" href="estilos.css">
                    <title>Clases CSS</title>
                </head>
                <body>
                    <h2>Personalizar texto</h2>

                    <p id="mensaje">Este es un mensaje</p>

                    <br>

                    <button onclick="ponerRojo()">Asignar la clase 'rojo'</button>
                    <button onclick="cambiarTamanyoLetra()">Añadir la clase 'grande' al párrafo</button>
                    <button onclick="ponerBorde()">Añadir la clase 'borde' al párrafo</button>
                    <button onclick="reset()">Reset</button>
                </body>
            </html>
            ```
        
        === "estilos.css"

            ```css
            .rojo {
                color: red;
            }

            .azul {
                color: blue;
            }

            .grande {
                font-size: 30px;
            }

            .borde {
                border: 2px solid black;
                padding: 5px;
            }
            ```

        === "app.js"

            ```javascript
            function ponerRojo() {
                let texto;

                // 1. Obtener el elemento <p id="mensaje">Este es un mensaje</p> y almacenarlo en la variable 'texto'

                // 2. Asignar a la variable 'texto' la clase 'rojo'
            }

            function cambiarTamanyoLetra() {
                let texto;
                
                // 1. Obtener el elemento <p id="mensaje">Este es un mensaje</p> y almacenarlo en la variable 'texto'

                // 2. Añadir a la variable 'texto' la clase 'rojo'
            }

            function ponerBorde() {
                let texto;
                
                // 1. Obtener el elemento <p id="mensaje">Este es un mensaje</p> y almacenarlo en la variable 'texto'

                // 2. Añadir a la variable 'texto' la clase 'borde'
            }

            function reset() {
                let texto;
                
                // 1. Obtener el elemento <p id="mensaje">Este es un mensaje</p> y almacenarlo en la variable 'texto'

                // 2. Asignar a la variable 'texto' el texto ""
            }
            ```

        === "SOLUCIÓN"

            ```javascript
            function ponerRojo() {
                let texto;

                // 1. Obtener el elemento <p id="mensaje">Este es un mensaje</p> y almacenarlo en la variable 'texto'
                texto = document.querySelector("#mensaje");

                // 2. Asignar a la variable 'texto' la clase 'rojo'
                texto.className = "rojo";
            }

            function cambiarTamanyoLetra() {
                let texto;
                
                // 1. Obtener el elemento <p id="mensaje">Este es un mensaje</p> y almacenarlo en la variable 'texto'
                texto = document.querySelector("#mensaje");

                // 2. Añadir a la variable 'texto' la clase 'rojo'
                texto.classList.add("grande");
            }

            function ponerBorde() {
                let texto;
                
                // 1. Obtener el elemento <p id="mensaje">Este es un mensaje</p> y almacenarlo en la variable 'texto'
                texto = document.querySelector("#mensaje");

                // 2. Añadir a la variable 'texto' la clase 'borde'
                texto.classList.add("borde");
            }

            function reset() {
                let texto;
                
                // 1. Obtener el elemento <p id="mensaje">Este es un mensaje</p> y almacenarlo en la variable 'texto'
                texto = document.querySelector("#mensaje");

                // 2. Asignar a la variable 'texto' el texto ""
                texto.className = "";
            }
            ```


### 5.4.6 Repaso

!!! note "Ejercicios Repaso"

    ??? example "Ejercicio 11: Reserva Actividades"
                
        === "index.html"

            ```html
            <!DOCTYPE html>
            <html lang="es">
                <head>
                    <meta charset="UTF-8">
                    <title>Reserva de actividades</title>
                    <script src="app.js" defer></script>
                    <link rel="stylesheet" href="estilos.css">
                </head>
                <body>
                    <h2>Reserva de actividades</h2>

                    <div class="contenedor">

                        <input type="text" id="nombre" placeholder="Nombre">

                        <div class="grupo">
                            <strong>Actividad:</strong><br>
                            <input type="radio" name="actividad" class="actividad ancho" value="Yoga"> Yoga<br>
                            <input type="radio" name="actividad" class="actividad ancho" value="Spinning"> Spinning<br>
                        </div>

                        <div class="grupo">
                            <strong>Extras:</strong><br>
                            <input type="checkbox" class="extra ancho" value="Agua"> Agua<br>
                            <input type="checkbox" class="extra ancho" value="Toalla"> Toalla<br>
                        </div>

                        <select id="horario">
                            <option value="">Selecciona horario</option>
                            <option value="Mañana">Mañana</option>
                            <option value="Tarde">Tarde</option>
                        </select>

                        <br>

                        <button onclick="reservar()">Reservar</button>

                    </div>

                    <ul id="lista"></ul>
                </body>
            </html>
            ```
        
        === "estilos.css"

            ```css
            body {
                font-family: Arial;
                background: #f0f2f5;
                text-align: center;
            }

            .contenedor {
                background: white;
                padding: 20px;
                width: 450px;
                margin: auto;
                border-radius: 10px;
                box-shadow: 0 4px 10px rgba(0,0,0,0.1);
            }

            input, select {
                margin: 10px;
                padding: 8px;
                width: 80%;
            }

            .ancho {
                margin: 10px;
                padding: 8px;
                width: 10%;
            }

            .grupo {
                text-align: left;
                margin-left: 40px;
            }

            button {
                padding: 10px;
                background: green;
                color: white;
                border: none;
                cursor: pointer;
            }

            ul {
                background: white;
                width: 60%;
                margin: 20px auto;
                padding: 10px;
                border-radius: 10px;
            }

            li {
                margin: 5px;
            }

            .aviso {
                color: red;
                font-weight: bold;
            }

            .ok {
                color: green;
            }

            ul {
            list-style: none;
            }
            ```

        === "app.js"

            ```javascript
            function reservar() {
                let nombre="", actividad="", textoExtras="", horario="";
                let radios, extras, elementoLI, lista;
                let i = 0;

                //1. Obtener el elemento con id='nombre' y almacenarlo en la variable 'nombre'.

                //2. Obtener los elementos que contengan la clase: class='actividad' y almacenarlos en la variable 'radios'.

                //3. Recorrer todos los elementos de la variable 'radios' y comprobar qué elemento está seleccionado. 
                // Almacenar el value del elemento seleccionado en la variable 'actividad'.

                //4. Obtener los elementos que contengan la clase: class='extra' y almacenarlos en la variable 'extras'.

                //5. Asignar el valor 0 a la variable 'i'.

                //6. Recorrer todos los elementos de la variable 'extras' y almacenar los valores (value) de los elementos que están chequeados.
                // Los valores se deberán almacenar en la variable 'textoExtras' empleando la siguiente expresión: textoExtras = textoExtras + extras[i].value

                //7. Si la variable 'textoExtras' es igual a "", asignar a la variable textoExtras el texto: "Sin extras";

                //8. Obtener el valor (value) del elemento <select id="horario"> y almacenarlo en la variable 'horario'.

                //10. Crear un elemento 'li' y almacenarlo en la variable 'elementoLI.

                //11. Asignar el texto: nombre + " - " + actividad + " - " + horario + " - " + textoExtras, en la propiedad textContent de la variable 'elementoLI'

                //12. obtener el elemento con id="lista y almacenarlo en la variable lista.

                //13. Si la variable 'horario' es igual al texto 'Mañana', añadir a la variable 'lista' el elemento 'elementoLI' (el elemento 'elementoLI' debe aparecer al principio de la lista),
                // sino, añadir a la variable 'lista' el elemento 'elementoLI' pero el 'elementoLI' debe aparecer al final de la lista.
                
            }
            ```

        === "SOLUCIÓN"

            ```javascript
            function reservar() {
                let nombre="", actividad="", textoExtras="", horario="";
                let radios, extras, elementoLI, lista;
                let i = 0;

                //1. Obtener el elemento <input type="text" id="nombre" placeholder="Nombre"> y almacenarlo en la variable 'nombre'.
                nombre = document.querySelector("#nombre").value;

                //2. Obtener los elementos que contengan la clase: class='actividad' y almacenarlos en la variable 'radios'.
                radios = document.querySelectorAll(".actividad");

                //3. Recorrer todos los elementos de la variable 'radios' y comprobar qué elemento está seleccionado. 
                // Almacenar el value del elemento seleccionado en la variable 'actividad'.
                while(i < radios.length)
                {
                    if(radios[i].checked){
                        actividad = radios[i].value;
                    }
                    i++;
                }

                //4. Obtener los elementos que contengan la clase: class='extra' y almacenarlos en la variable 'extras'.
                extras = document.querySelectorAll(".extra");

                //5. Asignar el valor 0 a la variable 'i'.
                i = 0;

                //6. Recorrer todos los elementos de la variable 'extras' y almacenar los valores (value) de los elementos que están chequeados.
                // Los valores se deberán almacenar en la variable 'textoExtras' empleando la siguiente expresión: textoExtras = textoExtras + extras[i].value
                while(i < extras.length)
                {
                    if(extras[i].extras){
                        textoExtras = textoExtras + extras[i].value
                    }
                    i++;
                }

                //7. Si la variable 'textoExtras' es igual a "", asignar a la variable textoExtras el texto: "Sin extras";
                if (textoExtras === "") {
                    textoExtras = "Sin extras";
                }

                //8. Obtener el valor (value) del elemento <select id="horario"> y almacenarlo en la variable 'horario'.
                horario = document.querySelector("#horario").value;

                //10. Crear un elemento 'li' y almacenarlo en la variable 'elementoLI.
                elementoLI = document.createElement("li");

                //11. Asignar el texto: nombre + " - " + actividad + " - " + horario + " - " + textoExtras, en la propiedad textContent de la variable 'elementoLI'
                elementoLI.textContent = nombre + " - " + actividad + " - " + horario + " - " + textoExtras;

                //12. obtener el elemento <ul id="lista"></ul> y almacenarlo en la variable lista.
                lista = document.querySelector("#lista");

                //13. Si la variable 'horario' es igual al texto 'Mañana', añadir a la variable 'lista' el elemento 'elementoLI' (el elemento 'elementoLI' debe aparecer al principio de la lista),
                // sino, añadir a la variable 'lista' el elemento 'elementoLI' pero el 'elementoLI' debe aparecer al final de la lista.
                if(horario === "Mañana"){
                    lista.prepend(elementoLI);
                }
                else{
                    lista.append(elementoLI);
                }
            }
            ```

    ??? example "Ejercicio 12: Reserva tu Viaje"
                
        === "index.html"

            ```html
            <!DOCTYPE html>
            <html lang="es">
                <head>
                    <meta charset="UTF-8">
                    <script src="app.js" defer></script>
                    <link rel="stylesheet" href="estilos.css">
                    <title>Reserva de viaje</title>
                </head>
                <body>
                    <h2>🌍 Reserva tu viaje</h2>

                    <div class="contenedor">
                        <!-- FORMULARIO -->
                        <div class="formulario">

                            <h3>Datos del viajero</h3>

                            <input type="text" id="nombre" placeholder="Nombre">
                            <input type="email" id="email" placeholder="Email">

                            <div class="grupo">
                                <strong>Destino:</strong><br>
                                <input type="radio" class="ancho destino" name="destino" value="París"> París<br>
                                <input type="radio" class="ancho destino" name="destino" value="Roma"> Roma<br>
                            </div>

                            <div class="grupo">
                                <strong>Actividades:</strong><br>
                                <input type="checkbox" class="actividad ancho" value="Museos"> Museos<br>
                                <input type="checkbox" class="actividad ancho" value="Excursiones"> Excursiones<br>
                            </div>

                            <select id="alojamiento">
                                <option value="">Alojamiento</option>
                                <option value="Hotel">Hotel</option>
                                <option value="Apartamento">Apartamento</option>
                            </select>

                            <textarea id="observaciones" placeholder="Observaciones..."></textarea>

                            <button onclick="reservar()">Reservar</button>

                        </div>

                        <!-- RESUMEN -->
                        <div class="resumen">
                            <h3>Reservas realizadas</h3>
                            <ul id="lista"></ul>
                        </div>
                    </div>
                </body>
            </html>
            ```
        
        === "estilos.css"

            ```css
            body {
                font-family: 'Segoe UI', sans-serif;
                background: linear-gradient(to right, #74ebd5, #9face6);
                margin: 0;
                padding: 20px;
            }

            h2 {
                text-align: center;
                color: white;
            }

            .contenedor {
                display: flex;
                gap: 20px;
                justify-content: center;
                flex-wrap: wrap;
            }

            .formulario, .resumen {
                background: white;
                padding: 20px;
                border-radius: 15px;
                width: 350px;
                box-shadow: 0 6px 15px rgba(0,0,0,0.2);
            }

            .formulario h3 {
                margin-top: 0;
            }

            input, select, textarea {
                width: 100%;
                padding: 8px;
                margin: 8px 0;
                border-radius: 5px;
                border: 1px solid #ccc;
            }

            .ancho {
                width: 10%;
            }

            .grupo {
                margin: 10px 0;
            }

            button {
                width: 100%;
                padding: 10px;
                background: #6c63ff;
                color: white;
                border: none;
                border-radius: 8px;
                cursor: pointer;
            }

            button:hover {
                background: #4a47c9;
            }

            ul {
                list-style: none;
                padding: 0;
            }

            li {
                background: #f7f7f7;
                margin: 8px 0;
                padding: 10px;
                border-radius: 8px;
            }

            .error {
                color: red;
                font-weight: bold;
            }

            .ok {
                color: green;
            }

            .top-msg {
                background: yellow;
                padding: 5px;
                margin-bottom: 10px;
            }
            ```

        === "app.js"

            ```javascript
            function reservar() {
                let nombre = "", email = "", destino = "", textoActividades ="";
                let alojamiento = "", observaciones = "";
                let radios, actividades, li, error, nota, lista;
                let i = 0;

                //1. Obtener el valor del elemento con id="nombre" y almacenarlo en la variable 'nombre'.

                //2. Obtener el valor del elemento con id="email" y almacenarlo en la variable 'email'.

                //3. Obtener los elementos que contengan la clase: class='destino' y almacenarlos en la variable 'radios'.

                //4. Recorrer todos los elementos de la variable 'radios' y comprobar qué elemento está seleccionado. 
                // Almacenar el value del elemento seleccionado en la variable 'destino'.

                //5. Obtener los elementos que contengan la clase: class='actividad' y almacenarlos en la variable 'actividades'.

                //6. Asignar el valor 0 a la variable 'i'.

                //7. Recorrer todos los elementos de la variable 'actividades' y almacenar los valores (value) de los elementos que están chequeados.
                // Los valores se deberán almacenar en la variable 'textoActividades' empleando la siguiente expresión: textoActividades = textoActividades + extras[i].value

                //8. Si la variable 'textoActividades' es igual a "", asignar a la variable textoExtras el texto: "Sin actividades";

                //9. Obtener el valor (value) del elemento <select id="alojamiento"> y almacenarlo en la variable 'alojamiento'.

                //10. Obtener el valor (value) del elemento <textarea id="observaciones"> y almacenarlo en la variable 'observaciones'.

                //11. Crear un elemento 'li' y almacenarlo en la variable 'li'.

                //12. Asignar el texto: nombre + " (" + email + ") → " + destino + " | " + alojamiento + " | " + textoActividades, en la propiedad textContent de la variable 'li'

                //13. obtener el elemento con id="lista" y almacenarlo en la variable lista.

                //14. Añadir a la variable lista la variable li. El elemento 'li' se debe añadir al final del elemento lista (append).

                //15. Si destino es igual a "" se deben realizar las siguientes tareas:
                //     a) Crear un elemento 'p' y almacenarlo en la variable 'error'.
                //     b) Añadir a la variable 'error' el texto "⚠ Falta seleccionar destino" (textContent)
                //     c) Añadir a la variable 'error' la clase CSS con el nombre 'error'.
                //     d) Añadir a la variable 'li' la clase CSS con el nombre 'error'.
                //     e) Añadir a la variable 'li' la variable error. La variable error debe aparecer en pantalla al principio de la variable li (before).
                

                //16. Si observaciones es igual a "" se deben realizar las siguientes tareas:
                //     a) Crear un elemento 'p' y almacenarlo en la variable 'nota'.
                //     b) Añadir a la variable 'nota' el texto "📝 Observaciones añadidas" (textContent)
                //     c) Añadir a la variable 'nota' la clase CSS con el nombre 'ok'.
                //     d) Añadir a la variable 'li' la clase CSS con el nombre 'ok'.
                //     e) Añadir a la variable 'li' la variable 'nota'. La variable nota debe aparecer en pantalla al principio de la variable li (before).
                
            }
            ```

        === "SOLUCIÓN"

            ```javascript
            function reservar() {
                let nombre = "", email = "", destino = "", textoActividades ="";
                let alojamiento = "", observaciones = "";
                let radios, actividades, li, error, nota, lista;
                let i = 0;

                //1. Obtener el valor del elemento <input type="text" id="nombre" placeholder="Nombre"> y almacenarlo en la variable 'nombre'.
                nombre = document.querySelector("#nombre").value;

                //2. Obtener el valor del elemento <input type="email" id="email" placeholder="Email"> y almacenarlo en la variable 'email'.
                email = document.querySelector("#email").value;

                //3. Obtener los elementos que contengan la clase: class='destino' y almacenarlos en la variable 'radios'.
                radios = document.querySelectorAll(".destino");

                //4. Recorrer todos los elementos de la variable 'radios' y comprobar qué elemento está seleccionado. 
                // Almacenar el value del elemento seleccionado en la variable 'destino'.
                while(i < radios.length)
                {
                    if(radios[i].checked){
                        destino = radios[i].value;
                    }
                    i++;
                }

                //5. Obtener los elementos que contengan la clase: class='actividad' y almacenarlos en la variable 'actividades'.
                actividades = document.querySelectorAll(".actividad");

                //6. Asignar el valor 0 a la variable 'i'.
                i = 0;

                //7. Recorrer todos los elementos de la variable 'actividades' y almacenar los valores (value) de los elementos que están chequeados.
                // Los valores se deberán almacenar en la variable 'textoActividades' empleando la siguiente expresión: textoActividades = textoActividades + extras[i].value
                while(i < actividades.length)
                {
                    if(actividades[i].checked){
                        textoActividades = textoActividades + actividades[i].value
                    }
                    i++;
                }

                //8. Si la variable 'textoActividades' es igual a "", asignar a la variable textoExtras el texto: "Sin actividades";
                if (textoActividades === ""){
                    textoActividades = "Sin actividades";
                }

                //9. Obtener el valor (value) del elemento <select id="alojamiento"> y almacenarlo en la variable 'alojamiento'.
                alojamiento = document.querySelector("#alojamiento").value;

                //10. Obtener el valor (value) del elemento <textarea id="observaciones"> y almacenarlo en la variable 'observaciones'.
                observaciones = document.querySelector("#observaciones").value;

                //11. Crear un elemento 'li' y almacenarlo en la variable 'li'.
                li = document.createElement("li");

                //12. Asignar el texto: nombre + " (" + email + ") → " + destino + " | " + alojamiento + " | " + textoActividades, en la propiedad textContent de la variable 'li'
                li.textContent = nombre + " (" + email + ") → " + destino + " | " + alojamiento + " | " + textoActividades;

                //13. obtener el elemento <ul id="lista"></ul> y almacenarlo en la variable lista.
                lista = document.querySelector("#lista");

                //14. Añadir a la variable lista la variable li. El elemento 'li' se debe añadir al final del elemento lista (append).
                lista.append(li);

                //15. Si destino es igual a "" se deben realizar las siguientes tareas:
                //     a) Crear un elemento 'p' y almacenarlo en la variable 'error'.
                //     b) Añadir a la variable 'error' el texto "⚠ Falta seleccionar destino" (textContent)
                //     c) Añadir a la variable 'error' la clase CSS con el nombre 'error'.
                //     d) Añadir a la variable 'li' la clase CSS con el nombre 'error'.
                //     e) Añadir a la variable 'li' la variable error. La variable error debe aparecer en pantalla al principio de la variable li (before).
                if (destino === "") {
                    error = document.createElement("p");
                    error.textContent = "⚠ Falta seleccionar destino";
                    error.classList.add("error");

                    li.classList.add("error");
                    li.before(error);
                }

                //16. Si observaciones es igual a "" se deben realizar las siguientes tareas:
                //     a) Crear un elemento 'p' y almacenarlo en la variable 'nota'.
                //     b) Añadir a la variable 'nota' el texto "📝 Observaciones añadidas" (textContent)
                //     c) Añadir a la variable 'nota' la clase CSS con el nombre 'ok'.
                //     d) Añadir a la variable 'li' la clase CSS con el nombre 'ok'.
                //     e) Añadir a la variable 'li' la variable 'nota'. La variable nota debe aparecer en pantalla al principio de la variable li (before).
                if (observaciones !== "") {
                    nota = document.createElement("p");
                    nota.textContent = "📝 Observaciones añadidas";
                    nota.classList.add("ok");

                    li.classList.add("ok");
                    li.before(nota);
                }
            }
            ```


### 5.4.7 Resumen

!!! note "Ejercicios con diferentes funciones"

    ??? example "Ejercicio 13: Lista Compra"
                
        === "index.html"

            ```html
            <!DOCTYPE html>
            <html lang="es">
                <head>
                    <meta charset="UTF-8">
                    <script src="app.js" defer></script>
                    <link rel="stylesheet" href="estilos.css">
                    <title>Gestión de productos</title>
                </head>
                <body>
                    <h2>Gestión de productos</h2>

                    <div class="contenedor">
                        <input type="text" id="nombre" placeholder="Nombre del producto">

                        <input type="text" id="precio" placeholder="Precio del producto">

                        <select id="cantidad">
                            <option value="1">1 Kilo</option>
                            <option value="2">2 kilos</option>
                            <option value="3">3 kilos</option>
                            <option value="4">4 kilos</option>
                            <option value="5">5 kilos</option>
                        </select>

                        <div class="ofertas">
                            <strong>Oferta:</strong><br>
                            <input type="radio" class="oferta ancho" name="oferta" value="si"> Sí (dto 50%)<br>
                            <input type="radio" class="oferta ancho" name="oferta" value="no"> NO (dto 0%)<br>
                        </div>

                        <br>

                        <button onclick="anyadirProducto()">Añadir producto</button>
                    </div>

                    <h3>Lista</h3>
                    <ul id="lista"></ul>
                </body>
            </html>
            ```
        
        === "estilos.css"

            ```css
            body {
                font-family: Arial, sans-serif;
                background: #f4f6f8;
                text-align: center;
                margin: 0;
                padding: 20px;
            }

            h2 {
                color: #333;
            }

            .contenedor {
                background: white;
                padding: 20px;
                border-radius: 10px;
                width: 400px;
                margin: auto;
                box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
            }

            input,
            select {
                padding: 8px;
                margin: 10px;
                width: 80%;
                border-radius: 5px;
                border: 1px solid #ccc;
            }

            button {
                padding: 10px 20px;
                background: #4caf50;
                color: white;
                border: none;
                border-radius: 5px;
                cursor: pointer;
            }

            button:hover {
                background: #45a049;
            }

            table {
                margin: 20px auto;
                border-collapse: collapse;
                width: 60%;
                background: white;
                box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
            }

            th {
                background: #4caf50;
                color: white;
                padding: 10px;
            }

            td {
                padding: 10px;
                border-bottom: 1px solid #ddd;
            }

            tr:hover {
                background: #f1f1f1;
            }

            .ofertas {
                text-align: left;
                margin-left: 40px;
            }

            .ancho {
                margin: 10px;
                padding: 8px;
                width: 10%;
            }

            ul {
                list-style: none;
            }
            ```

        === "app.js"

            ```javascript
            // TAREAS A REALIZAR:
            //  a) Botón 'Añadir Producto': Añade un nuevo producto a la lista con los datos introducidos.

            function anyadirProducto() {
                let nombre = "", textoOferta = "";
                let nombreProducto, elementoLI, lista, ofertas;
                let precio = 0, total = 0, i = 0, cantidad = 0;

       
                // ESCRIBIR EL CÓDIGO NECESARIO
            }
            ```

        === "SOLUCIÓN"

            ```javascript
            function anyadirProducto() {
                let nombre = "", textoOferta = "";
                let nombreProducto, elementoLI, lista, ofertas;
                let precio = 0, total = 0, i = 0, cantidad = 0;

                // 1. Obtener el valor del elemento con id="nombre" y almacenarlo en la variable 'nombre'
                nombre = document.querySelector("#nombre").value;

                // 2. Obtener el valor del elemento con id="cantidad" y almacenarlo en la variable 'nombre'
                cantidad = parseFloat(document.querySelector("#cantidad").value);

                // 3. Obtener el valor del elemento con id="precio" y almacenarlo en la variable 'precio'
                precio = parseFloat(document.querySelector("#precio").value);

                // 4. Obtener el valor del elemento con id="precio" y almacenarlo en la variable 'precio'
                total = cantidad * precio;

                // 5. Obtener la oferta seleccionada. Guardar la selección en la variable 'tectoOferta'
                ofertas = document.querySelectorAll(".oferta");

                while(i < ofertas.length) {
                    if(ofertas[i].checked){
                        textoOferta = ofertas[i].value;
                    }
                    i++;
                }

                // 6. Si La oferta es igual a 'Sí'. Descontar el 50% al total.
                if(textoOferta == "si"){
                    total = total * 0.5;
                }

                // 7. Borrar el nombre y el precio de la pantalla para que queden en blanco.
                document.querySelector("#nombre").value = "";
                document.querySelector("#precio").value = "";

                //8. Crear un elemento 'li' y almacenarlo en la variable 'elementoLI.
                elementoLI = document.createElement("li");

                //9. Asignar el texto: nombre + " - " + precio + " - " + cantidad + " - " + total;
                elementoLI.textContent = nombre + " - " + precio + " - " + cantidad + " - " + textoOferta + " - " + total;

                //10. obtener el elemento <ul id="lista"></ul> y almacenarlo en la variable lista.
                lista = document.querySelector("#lista");

                //11. Añadir la variable 'elementoLI' a la lista.
                lista.append(elementoLI);
            }
            ```

    ??? example "Ejercicio 14: Lista Compra v2"
                
        === "index.html"

            ```html
            <!DOCTYPE html>
            <html lang="es">

            <head>
                <meta charset="UTF-8">
                <title>Gestión de productos</title>
                <script src="app.js" defer></script>
                <link rel="stylesheet" href="estilos.css">
            </head>

            <body>
                <h2>Gestión de productos</h2>

                <div class="contenedor">
                    <input type="text" id="nombre" placeholder="Nombre del producto">

                    <input type="number" id="precio" placeholder="Precio (€)">

                    <select id="cantidad">
                        <option value="1">1 Kilo</option>
                        <option value="2">2 kilos</option>
                        <option value="3">3 kilos</option>
                        <option value="4">4 kilos</option>
                        <option value="5">5 kilos</option>
                    </select>

                    <br>

                    <div class="ofertas">
                        <strong>Oferta:</strong><br>
                        <input type="radio" class="oferta ancho" name="oferta" value="si"> Sí (dto 50%)<br>
                        <input type="radio" class="oferta ancho" name="oferta" value="no"> NO (dto 0%)<br>
                    </div>

                    <button onclick="anyadirProducto()">Añadir Producto</button>
                    <button onclick="borrarProducto()">Borrar Producto</button>
                    <button onclick="calcularTotal()">Calcular suma total</button>
                </div>

                <table id="tabla">
                    <thead>
                        <tr>
                            <th>Producto</th>
                            <th>Precio (€)</th>
                            <th>Cantidad</th>
                            <th>Oferta</th>
                            <th>Total (€)</th>
                        </tr>
                    </thead>
                    <tbody id="tbody">
                    </tbody>
                </table>

                <p id="totalFinal"></p>
            </body>

            </html>
            ```
        
        === "estilos.css"

            ```css
            body {
                font-family: Arial, sans-serif;
                background: #f4f6f8;
                text-align: center;
                margin: 0;
                padding: 20px;
            }

            h2 {
                color: #333;
            }

            .contenedor {
                background: white;
                padding: 20px;
                border-radius: 10px;
                width: 400px;
                margin: auto;
                box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
            }

            input,
            select {
                padding: 8px;
                margin: 10px;
                width: 80%;
                border-radius: 5px;
                border: 1px solid #ccc;
            }

            button {
                padding: 10px 20px;
                margin: 5px;
                background: #4caf50;
                color: white;
                border: none;
                border-radius: 5px;
                cursor: pointer;
            }

            button:hover {
                background: #45a049;
            }

            table {
                margin: 20px auto;
                border-collapse: collapse;
                width: 70%;
                background: white;
                box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
            }

            th {
                background: #4caf50;
                color: white;
                padding: 10px;
            }

            td {
                padding: 10px;
                border-bottom: 1px solid #ddd;
            }

            tr:hover {
                background: #f1f1f1;
            }

            #totalFinal {
                font-size: 18px;
                font-weight: bold;
                margin-top: 15px;
            }

            .ofertas {
                text-align: left;
                margin-left: 40px;
            }

            .ancho {
                margin: 10px;
                padding: 8px;
                width: 10%;
            }

            ul {
                list-style: none;
            }
            ```

        === "app.js"

            ```javascript
            // TAREAS A REALIZAR:
            //   a) funtion anyadirProducto():
                    // 1- Recuperar todos los elementos introducidos por teclado
                    // 2- Calcular el total: precio * cantidad.
                    // 3- Crear un elemento de tipo 'tr'.
                    // 4- Crear 5 elementos de tipo td.
                    // 5- Asignar a cada elemento td las siguients variables: nombre, precio, cantidad, textoOferta, total.
                    // 6- Asignar cada elemento td al elemento tr. (append)
                    // 6- Asignar la clase CSS 'total' al elemento creado td que contenga el 'total'.
                    // 7- Borrar los datos introducidos por teclado.

            function anyadirProducto() {
                let nombre = "", textoOferta = "";
                let precio = 0, cantidad = 0, total = 0, i = 0;
                let fila, celdaNombreTD, celdaPrecioTD, celdaCantidadTD, celdaTotalTD, tbody, inputNombre, inputPrecio, ofertas;


                // ESCRIBIR EL CÓDIGO NECESARIO
            }

            function calcularTotal() {
                let filas;
                let suma = 0, i = 0;

                // 1. Obtener todos los elementos que tengan la clase: class="total". Guardar todas las filas de la tabla 
                // en la variable 'filas'.
                filas = document.querySelectorAll(".total");

                // 2. Recorrer todos las filas y sumar todos los totales. La suma de los totales se debe guardar 
                // en la variable 'suma'.
                while (i < filas.length) {
                    suma = suma + parseFloat(filas[i].textContent);
                    i++;
                }

                // 3. Mostrar en el elemento con id=totalFinal el mensaje: "Total A Pagar: " + suma + " €"
                document.querySelector("#totalFinal").textContent = "TOTAL GENERAL: " + suma.toFixed(2) + " €";
            }

            function borrarProducto() {
                let tbody;

                // 1. Obtener todos los hijos del elemento con id='tbody' (las filas de la tabla)
                filas = document.querySelector("#tbody").children;

                // 2. Si la variable 'filas' tiene una longitud (length) > a cero, se debe borrar el primer elemento de la variable filas: filas[0]
                if (filas.length > 0) {
                    filas[0].remove();
                }
            }
            ```
        
        === "SOLUCIÓN"

            ```javascript
            function anyadirProducto() {
                let nombre = "", textoOferta = "";
                let precio = 0, cantidad = 0, total = 0, i = 0;
                let fila, celdaNombreTD, celdaPrecioTD, celdaCantidadTD, celdaTotalTD, tbody, inputNombre, inputPrecio, ofertas;

                // 1. Obtener el valor del elemento con id='nombre'. Guardar el nombre en la variable 'nombre'.
                nombre = document.querySelector("#nombre").value;

                // 2. Obtener el valor del elemento con id="precio". Guardar el precio en la variable 'precio'.
                precio = parseFloat(document.querySelector("#precio").value);

                // 3. Obtener el valor del elemento con id="cantidad". Guardar la cantidad en la variable 'cantidad'.
                cantidad = parseInt(document.querySelector("#cantidad").value);

                // 5. Multiplicar el precio * cantidad y guardar el resultado en la variable 'total'.
                total = precio * cantidad;

                // 6. Obtener la oferta seleccionada. Guardar la selección en la variable 'tectoOferta'
                ofertas = document.querySelectorAll(".oferta");

                while (i < ofertas.length) {
                    if (ofertas[i].checked) {
                        textoOferta = ofertas[i].value;
                    }
                    i++;
                }

                // 7. Si La oferta es igual a 'Sí'. Descontar el 50% al total.
                if (textoOferta == "si") {
                    total = total * 0.5;
                }

                // 8. Crear un elemento de tipo 'tr'. Guardar el elemento en la variable 'fila'.
                fila = document.createElement("tr");

                // 9. Crear un elemento de tipo 'td'. Guardar el elemento en la variable 'celdaNombreTD'.
                celdaNombreTD = document.createElement("td");

                // 10. Asignar al 'textContent' del elemento 'celdaNombreTD' la variable 'nombre'.
                celdaNombreTD.textContent = nombre;

                // 11. Crear un elemento de tipo 'td'. Guardar el elemento en la variable 'celdaPrecioTD'.
                celdaPrecioTD = document.createElement("td");

                // 12. Asignar al 'textContent' del elemento 'celdaPrecioTD' la variable 'precio'.
                celdaPrecioTD.textContent = precio.toFixed(2);

                // 13. Crear un elemento de tipo 'td'. Guardar el elemento en la variable 'celdaCantidadTD'.
                celdaCantidadTD = document.createElement("td");

                // 14. Asignar al 'textContent' del elemento 'celdaCantidadTD' la variable 'cantidad'.
                celdaCantidadTD.textContent = cantidad;

                // 15. Crear un elemento de tipo 'td'. Guardar el elemento en la variable 'celdaCantidadTD'.
                celdaOfertaTD = document.createElement("td");

                // 16. Asignar al 'textContent' del elemento 'celdaCantidadTD' la variable 'cantidad'.
                celdaOfertaTD.textContent = textoOferta;

                // 17. Crear un elemento de tipo 'td'. Guardar el elemento en la variable 'celdaTotalTD'.
                celdaTotalTD = document.createElement("td");

                // 18. Asignar al 'textContent' del elemento 'celdaTotalTD' la variable 'total'.
                celdaTotalTD.textContent = total.toFixed(2);

                // 19. Añadir la clase 'total' al elemento celdaTotalTD. La clase se empleará para calcular la suma.
                celdaTotalTD.classList.add("total");

                // 20. Añadir al elemento 'fila' el elemento 'celdaNombreTD'. El elemento celdaNombreTD se debe añadir al final. (append)
                fila.append(celdaNombreTD);

                // 21. Añadir al elemento 'fila' el elemento 'celdaPrecioTD'.  El elemento celdaPrecioTD se debe añadir al final
                fila.append(celdaPrecioTD);

                // 22. Añadir al elemento 'fila' el elemento 'celdaCantidadTD'. El elemento celdaCantidadTD se debe añadir al final
                fila.append(celdaCantidadTD);

                // 22. Añadir al elemento 'fila' el elemento 'celdaCantidadTD'. El elemento celdaCantidadTD se debe añadir al final
                fila.append(celdaOfertaTD);

                // 23. Añadir al elemento 'fila' el elemento 'celdaTotalTD'. El elemento celdaTotalTD se debe añadir al final
                fila.append(celdaTotalTD);

                // 24. Obtener el elemento con id="tbody" y almacenarlo en la variable tbody.
                tbody = document.querySelector("#tbody");

                // 25. Añadir al elemento tbody el elemento 'fila'. La final se debe añadir al final.
                tbody.append(fila);

                // 26. Borrar el nombre y el precio de la pantalla para que queden en blanco.
                document.querySelector("#nombre").value = "";
                document.querySelector("#precio").value = "";
            }

            function calcularTotal() {
                let filas;
                let suma = 0, i = 0;

                // 1. Obtener todos los elementos que tengan la clase: class="total". Guardar todas las filas de la tabla 
                // en la variable 'filas'.
                filas = document.querySelectorAll(".total");

                // 2. Recorrer todos las filas y sumar todos los totales. La suma de los totales se debe guardar 
                // en la variable 'suma'.
                while (i < filas.length) {
                    suma = suma + parseFloat(filas[i].textContent);
                    i++;
                }

                // 3. Mostrar en el elemento con id=totalFinal el mensaje: "Total A Pagar: " + suma + " €"
                document.querySelector("#totalFinal").textContent = "TOTAL GENERAL: " + suma.toFixed(2) + " €";
            }

            function borrarProducto() {
                let tbody;

                // 1. Obtener todos los hijos del elemento con id='tbody' (las filas de la tabla)
                filas = document.querySelector("#tbody").children;

                // 2. Si la variable 'filas' tiene una longitud (length) > a cero, se debe borrar el primer elemento de la variable filas: filas[0]
                if (filas.length > 0) {
                    filas[0].remove();
                }
            }
            ```

    ??? example "Ejercicio 15: Comanda"
                
        === "index.html"

            ```html
            <!DOCTYPE html>
            <html lang="es">
                <head>
                    <meta charset="UTF-8">
                    <script src="app.js" defer></script>
                    <link rel="stylesheet" href="estilos.css">
                    <title>Comandas con extras</title>
                </head>

                <body>
                    <h2>🍽️ Comandas con extras</h2>

                    <div class="contenedor">

                        <select id="plato">
                            <option value="">Selecciona plato</option>
                            <option value="10">Hamburguesa</option>
                            <option value="8">Pizza</option>
                            <option value="5">Ensalada</option>
                        </select>

                        <input type="number" id="precio" placeholder="Precio">

                        <input type="number" id="cantidad" placeholder="Cantidad">

                        <div class="extras">
                            <strong>Extras:</strong><br>
                            <input type="checkbox" class="extra" value="2"> Queso (+2€)<br>
                            <input type="checkbox" class="extra" value="1"> Salsa (+1€)<br>
                            <input type="checkbox" class="extra" value="3"> Bacon (+3€)<br>
                        </div>

                        <br>

                        <button onclick="anyadir()">Añadir Plato</button>
                        <button onclick="borrar()">Borrar Plato</button>
                        <button onclick="calcularTotal()">Total Comanda</button>
                    </div>

                    <table id="tabla">
                        <thead>
                            <tr>
                                <th>Plato</th>
                                <th>Precio base</th>
                                <th>Extras</th>
                                <th>Cantidad</th>
                                <th>Total</th>
                            </tr>
                        </thead>
                        <tbody id="tbody">
                        </tbody>
                    </table>

                    <p id="total"></p>
                </body>
            </html>
            ```
        
        === "estilos.css"

            ```css
            body {
                font-family: Arial;
                background: #f0f2f5;
                text-align: center;
                padding: 20px;
            }

            .contenedor {
                background: white;
                padding: 20px;
                width: 400px;
                margin: auto;
                border-radius: 10px;
                box-shadow: 0 4px 10px rgba(0,0,0,0.1);
            }

            select, #precio, #cantidad {
                width: 80%;
                padding: 8px;
                margin: 10px;
            }

            input {
                padding: 8px;
                margin: 10px;
            }

            .extras {
                text-align: left;
                margin-left: 40px;
            }

            button {
                padding: 10px;
                background: green;
                color: white;
                border: none;
                cursor: pointer;
            }

            table {
                margin: 20px auto;
                width: 70%;
                border-collapse: collapse;
                background: white;
            }

            th {
                background: black;
                color: white;
            }

            td, th {
                padding: 10px;
                border: 1px solid #ddd;
            }

            #total, #contador {
                font-weight: bold;
                font-size: 18px;
            }
            ```

        === "app.js"

            ```javascript
            // TAREAS A REALIZAR:
            //   a) function 'anyadir':
                    // 1- Recuperar todos los elementos introducidos por teclado.
                    // 2- Calcular el total: (precio + totalExtras) * cantidad;
                    // 3- Crear una fila (elemento tr). La fila tendrá los siguientes elementos td con las variables: plato, precio, totalExtras, cantidad y total;
                    // 4- Asignar la clase CSS 'total' al elemento creado td que contenga el 'total'.
                    // 5- Añadir la fila creada a la tabla con id=tbody
                    // 6- Borrar los datos introducidos por teclado.
            //   c) function 'borrar':
                    // 1- Recuperar todas las filas de la tabla.
                    // 2- Borrar la primera fila de la tabla.
            //   d) function 'calcularTotal':
                    // 1- Recuperar los elementos que cotengan la clase "total" 
                    // 2- Sumar todos los totales y mostrarlos.

            // 1. Actualiza el input 'precio'
            function anyadir() {
                let select, fila, tbody;
                let plato;
                let precio = 0, totalExtras = 0, cantidad = 0, i = 0, total = 0;
                let extras;

                // 1. Obtener el elemento con id="plato" y almacenarlo en la variable 'select'
                select = document.querySelector("#plato");

                // 2. Obtener el nombre del plato y guardarlo en la variable 'plato'
                plato = select.options[select.selectedIndex].text;

                // ESCRIBIR EL CÓDIGO NECESARIO

            }

            function calcularTotal() {
                let totales;
                let suma = 0, i = 0;

                // ESCRIBIR EL CÓDIGO NECESARIO

                // 3. Mostrar el mensaje: "TOTAL: " + suma + " €", en el elemento con id=total.
                document.querySelector("#total").textContent = "TOTAL: " + suma + " €";
            }

            function borrar(){
                let filas;

                // ESCRIBIR EL CÓDIGO NECESARIO

                // 3. LLamar al método calcularTotal() para actualizar el precio después de borrar un plato;
                calcularTotal();
            }
            ```

        === "SOLUCIÓN"

            ```javascript
            function anyadir() {
                let select, fila, tbody;
                let plato;
                let precio = 0, totalExtras = 0, cantidad = 0, i = 0, total = 0;
                let extras;

                // 1. Obtener el elemento con id="plato" y almacenarlo en la variable 'select'
                select = document.querySelector("#plato");

                // 2. Obtener el nombre del plato y guardarlo en la variable 'plato'
                plato = select.options[select.selectedIndex].text;

                // 3. Obtener el valor(value) del elemento con id="precio". Guardar el precio en la variable 'precio'.
                precio = parseFloat(document.querySelector("#precio").value);

                // 4. Obtener el valor(value) del elemento con id="cantidad". Guardar el precio en la variable 'cantidad'.
                cantidad = parseInt(document.querySelector("#cantidad").value);

                // 5. Obtener todos los elementos que tengan la clase: class="extra". Guardar los elementos en la clase 'extras'.
                extras = document.querySelectorAll(".extra");

                // 6. Recorrer todos los extra y sumar el precio de los extras seleccionados. La suma se debe almacenar en la variable 'totalExtras';
                while(i < extras.length){
                    if (extras[i].checked) {
                        totalExtras = totalExtras + parseFloat(extras[i].value);
                    }
                    i++;
                }

                // 7. Asignar la fórmula '(precio + totalExtras) * cantidad' en la variable 'total'.
                total = (precio + totalExtras) * cantidad;

                // 8. Crear un elemento 'tr' y asignarlos a la variable 'fila'.
                fila = document.createElement("tr");

                // 9. Crear 5 elementos 'td'. Cada elemento creado se asignará a las variables: celdPlatoTD, celdPrecioTD, celdaExtraTD, celdaCantidadTD, celdaTotalTD
                // A cada variable se le asignará en su atributo textContent las variables: plato, precio + ' €', totalExtras + ' €', cantidad y total + ' €', respectivamente.
                celdPlatoTD = document.createElement("td");
                celdPlatoTD.textContent = plato;
                celdPrecioTD = document.createElement("td");
                celdPrecioTD.textContent = precio + " €";
                celdaExtraTD = document.createElement("td");
                celdaExtraTD.textContent = totalExtras + " €";
                celdaCantidadTD = document.createElement("td");
                celdaCantidadTD.textContent = cantidad;
                celdaTotalTD = document.createElement("td");
                celdaTotalTD.textContent = total + " €";

                // 10. Añadir a la variable celdaTotalTD la clase CSS 'total'.
                celdaTotalTD.classList.add("total");

                // 11. Añadir a la variable 'fila', las variables: celdPlatoTD, celdPrecioTD, celdaExtraTD, celdaCantidadTD, celdaTotalTD.
                // Las variables añadidas se deben añadir una debajo de otra. (append)
                fila.append(celdPlatoTD);
                fila.append(celdPrecioTD);
                fila.append(celdaExtraTD);
                fila.append(celdaCantidadTD);
                fila.append(celdaTotalTD);

                // 12. Obtener el elemento con id ="tbody" y asignarlo a la variable tbody
                tbody = document.querySelector("#tbody");

                // 13. Añadir la variable tbody la variable fila. La fila se debe añadir después de todas las fila existentes.
                tbody.append(fila);

                // 14. Borrar todos los valores de los input.
                document.querySelector("#plato").value = "";
                document.querySelector("#precio").value = "";
                document.querySelector("#cantidad").value = "";
            }

            function calcularTotal() {
                let totales;
                let suma = 0, i = 0;

                // 1. Obtener los elementos con la clase: class="total"
                totales = document.querySelectorAll(".total");

                // 2. Recorrer todos los totales y sumar el total de cada producto. Almacenar la suma en la variable 'suma'.
                while(i < totales.length){
                    suma = suma + parseFloat(totales[i].textContent);
                    i++;
                }

                // 3. Mostrar el mensaje: "TOTAL: " + suma + " €", en el elemento con id=total.
                document.querySelector("#total").textContent = "TOTAL: " + suma + " €";
            }

            function borrar(){
                let filas;

                // 1. Obtener todas las filas hijas del elemento con id="tbody"
                filas = document.querySelector("#tbody").children;

                // 2. Si la variable 'filas' tiene una longitud (length) > a cero, se debe borrar el primer elemento de la variable filas: filas[0]
                if(filas.length > 0){
                    filas[0].remove();
                }

                // 3. LLamar al método calcularTotal();
                calcularTotal();
            }
            ```
    ??? example "Ejercicio 16: Consulta Médica"
                
        === "index.html"

            ```html
            <!doctype html>
            <html lang="es">
            <head>
                <meta charset="UTF-8" />
                <script src="app.js" defer></script>
                <link rel="stylesheet" href="estilos.css" />
                <title>Cita Médica</title>
            </head>
            <body>
                <div class="container">
                <h2>Reserva de Cita Médica</h2>

                <!-- Input 1 -->
                <label>Nombre del paciente:</label>
                <input type="text" id="nombre" />

                <!-- Input 2 -->
                <label>Fecha de la cita:</label>
                <input type="date" id="fecha" />

                <!-- Select -->
                <label>Especialidad:</label>
                <select id="especialidad">
                    <option value="Medicina general">Medicina general</option>
                    <option value="Dermatología">Dermatología</option>
                    <option value="Pediatría">Pediatría</option>
                </select>

                <!-- Radio -->
                <div class="radio-group">
                    <label>Tipo de consulta:</label>
                    <input type="radio" name="consulta" value="Presencial" />
                    Presencial<br />
                    <input type="radio" name="consulta" value="Online" /> Online
                </div>

                <button onclick="reservarCita()">Reservar</button>
                </div>
                <table id="tabla">
                <thead>
                    <tr>
                    <th>Nombre Paciente</th>
                    <th>Fecha</th>
                    <th>Especialidad</th>
                    <th>Tipo</th>
                    </tr>
                </thead>
                <tbody id="tbody"></tbody>
                </table>
            </body>
            </html>
            ```
        
        === "estilos.css"

            ```css
            body {
                font-family: Arial, sans-serif;
                background-color: #eef2f7;
                padding: 20px;
            }

            .container {
                background: white;
                padding: 20px;
                max-width: 400px;
                margin: auto;
                border-radius: 10px;
                box-shadow: 0 0 10px rgba(0,0,0,0.1);
            }

            h2 {
                text-align: center;
            }

            label {
                display: block;
                margin-top: 10px;
            }

            input, select {
                width: 100%;
                padding: 8px;
                margin-top: 5px;
            }

            .radio-group {
                margin-top: 10px;
            }

            button {
                margin-top: 15px;
                width: 100%;
                padding: 10px;
                background: #007bff;
                color: white;
                border: none;
                border-radius: 5px;
                cursor: pointer;
            }

            button:hover {
                background: #0056b3;
            }

            .resultado {
                margin-top: 15px;
                padding: 10px;
                background: #0056b3;
                border-radius: 5px;
            }

            table {
                margin: 20px auto;
                width: 70%;
                border-collapse: collapse;
                background: white;
            }

            th {
                background: #0056b3;
                color: white;
            }

            td, th {
                padding: 10px;
                border: 1px solid #ddd;
            }
            ```

        === "app.js"

            ```javascript
            // TAREAS A REALIZAR:
            //  a) Botón 'Añadir Producto': Añade un nuevo producto a la lista con los datos introducidos.

            function reservarCita() {
                //1 Recuperar todos los valores introducidos por teclado.

                //2 Crear una fila donde aparezcan todos los valores introducidos por teclado.
                // Cada valor se mostrará en una columna.

                //3 Añadir la fila a la tabla creada en el fichero html

                //4 Borrar los datos introducidos por el usuario.
            }
            ```

    ??? example "Ejercicio 17: Inscripción a actividades deportivas"
                
        === "index.html"

            ```html
            <!DOCTYPE html>
            <html lang="es">
                <head>
                    <meta charset="UTF-8">
                    <title>Actividades deportivas</title>
                    <script src="app.js" defer></script>
                    <link rel="stylesheet" href="estilos.css">
                </head>
                <body>
                    <h2>🏃 Inscripción deportiva</h2>

                    <div class="contenedor">
                        <b>Nombre:</b>
                        <input type="text" id="nombre" placeholder="Nombre">

                        <b>Actividad:</b>
                        <select id="actividad">
                            <option>Yoga</option>
                            <option>Natación</option>
                            <option>Running</option>
                        </select>

                        <br>
                        <b>Turno:</b>
                        <label><input type="radio" class="turno" name="turno" value="Mañana"> Mañana</label>
                        <label><input type="radio" class="turno" name="turno" value="Tarde"> Tarde</label>

                        <br>
                        <button onclick="agregar()">Inscribirse</button>

                    </div>

                    <table id="tabla">
                        <thead>
                            <tr>
                                <th>Nombre</th>
                                <th>Actividad</th>
                                <th>Turno</th>
                            </tr>
                        </thead>
                        <tbody id="tbody">
                        </tbody>
                    </table>
                </body>
            </html>
            ```
        
        === "estilos.css"

            ```css
            body {
                font-family: Arial;
                background: #eef2f7;
                text-align: center;
            }

            .contenedor {
                background: white;
                width: 400px;
                margin: auto;
                padding: 20px;
                border-radius: 10px;
                box-shadow: 0 4px 10px rgba(0,0,0,0.2);
            }

            input, select {
                width: 90%;
                padding: 8px;
                margin: 8px;
                border-radius: 5px;
                border: 1px solid #ccc;
            }

            button {
                padding: 10px 15px;
                background: #3b82f6;
                color: white;
                border: none;
                border-radius: 5px;
                cursor: pointer;
            }

            button:hover {
                background: #2563eb;
            }

            table {
                margin: 20px auto;
                border-collapse: collapse;
                background: white;
                width: 80%;
            }

            th {
                background: #3b82f6;
                color: white;
            }

            td, th {
                padding: 10px;
                border: 1px solid #ddd;
            }
            ```

        === "app.js"

            ```javascript
            // TAREAS A REALIZAR:
                //1 Recuperar todos los valores introducidos por teclado: nombre, actividad y turno.

                //2 Crear una fila donde aparezcan todos los valores introducidos por teclado.
                // Cada valor se mostrará en una columna y las columnas se añadirán a la fila creada.
                
                //3 Añadir la fila a la tabla creada en el fichero html

                //4 Borrar los datos introducidos por el usuario.

            function anyadirProducto() {
                let i = 0;
                let textoTurno = "";

       
                // ESCRIBIR EL CÓDIGO NECESARIO
            }
            ```

        === "SOLUCIÓN"

            ```javascript
            function agregar() {
                let i = 0;
                let textoTurno = "";

                let nombre = document.querySelector("#nombre").value;
                let actividad = document.querySelector("#actividad").value;

                let turnos = document.querySelectorAll(".turno");
                while(i < turnos.length){
                    if(turnos[i].checked){
                        textoTurno = turnos[i].value;
                    }

                    i++;
                }

                let fila = document.createElement("tr");
                let columna1 = document.createElement("td");
                let columna2 = document.createElement("td");
                let columna3 = document.createElement("td");

                columna1.textContent = nombre;
                columna2.textContent = actividad;
                columna3.textContent = textoTurno;

                fila.append(columna1);
                fila.append(columna2);
                fila.append(columna3);

                document.querySelector("#tbody").append(fila);
            }
            ```

    ??? example "Ejercicio 18: Reserva cine"
                
        === "index.html"

            ```html
            <!DOCTYPE html>
            <html lang="es">
                <head>
                    <meta charset="UTF-8">
                    <title>Reserva de cine</title>
                    <script src="app.js" defer></script>
                    <link rel="stylesheet" href="estilos.css">
                </head>
                <body>
                    <h2>🎬 Reserva de cine</h2>

                    <div class="contenedor">

                        <div class="grupo">
                            <label>Nombre</label>
                            <input type="text" id="nombre" name="nombre">
                        </div>

                        <div class="grupo">
                            <label>Película</label>
                            <select id="pelicula" name="pelicula">
                                <option>Acción</option>
                                <option>Comedia</option>
                                <option>Drama</option>
                            </select>
                        </div>

                        <div class="grupo">
                            <label>Horario</label>
                            <div class="opciones">
                                <label><input type="radio" class="horario" name="horario" value="Tarde"> Tarde</label>
                                <label><input type="radio" class="horario" name="horario" value="Noche"> Noche</label>
                            </div>
                        </div>

                        <div class="grupo">
                            <label>Extras</label>
                            <div class="opciones">
                                <label><input type="checkbox" class="extras" name="extras" value="Palomitas"> Palomitas</label>
                                <label><input type="checkbox" class="extras" name="extras" value="Bebida"> Bebida</label>
                            </div>
                        </div>

                        <button onclick="agregar()">Reservar</button>

                    </div>
                    <table id="tabla">
                        <thead>
                            <tr>
                                <th>Nombre</th>
                                <th>Película</th>
                                <th>Horario</th>
                                <th>Extra</th>
                            </tr>
                        </thead>
                        <tbody id="tbody">
                        </tbody>
                    </table>
                </body>
            </html>
            ```
        
        === "estilos.css"

            ```css
            body {
                font-family: Arial;
                background: linear-gradient(to right, #1e293b, #0f172a);
                color: white;
                text-align: center;
            }

            .contenedor {
                background: white;
                color: black;
                width: 420px;
                margin: 40px auto;
                padding: 25px;
                border-radius: 12px;
                box-shadow: 0 8px 20px rgba(0,0,0,0.4);
            }

            h2 {
                color: #f59e0b;
            }

            .grupo {
                margin: 15px 0;
                text-align: left;
            }

            label {
                display: block;
                margin-bottom: 5px;
                font-weight: bold;
            }

            input[type="text"], select {
                width: 100%;
                padding: 8px;
                border-radius: 6px;
                border: 1px solid #ccc;
            }

            .opciones {
                display: flex;
                gap: 10px;
            }

            button {
                width: 100%;
                padding: 10px;
                background: #f59e0b;
                color: white;
                border: none;
                border-radius: 6px;
                font-size: 16px;
                cursor: pointer;
            }

            button:hover {
                background: #d97706;
            }

            table {
                margin: 20px auto;
                border-collapse: collapse;
                width: 80%;
                background: white;
                color: black;
            }

            th {
                background: #f59e0b;
                color: white;
            }

            td, th {
                padding: 10px;
                border: 1px solid #ddd;
            }
            ```

        === "app.js"

            ```javascript
            // TAREAS A REALIZAR:
                //1 Recuperar todos los valores introducidos por teclado: nombre, pelicula, textoHorario, textoExtra.

                //2 Crear una fila donde aparezcan todos los valores introducidos por teclado.
                // Cada valor se mostrará en una columna y las columnas se añadirán a la fila creada.
                
                //3 Añadir la fila a la tabla creada en el fichero html

                //4 Borrar los datos introducidos por el usuario.

                function agregar() {
                    let i = 0;
                    let textoHorario = "", textoExtra = "";

                    // ESCRIBIR EL CÓDIGO NECESARIO
                }
            ```

        === "SOLUCIÓN"

            ```javascript
            function agregar() {
                let i = 0;
                let textoHorario = "", textoExtra = "";

                let nombre = document.querySelector("#nombre").value;
                    let pelicula = document.querySelector("#pelicula").value;

                    let horario = document.querySelectorAll(".horario");

                    while (i < horario.length) {
                        if (horario[i].checked) {
                            textoHorario = horario[i].value;
                        }
                        i++;
                    }


                    let extras = document.querySelectorAll(".extras");
                    i = 0;
                    while (i < extras.length) {
                        if (extras[i].checked) {
                            textoExtra = textoExtra + extras[i].value + " ";
                        }
                        i++;
                    }


                    let fila = document.createElement("tr");
                    let columna1 = document.createElement("td");
                    let columna2 = document.createElement("td");
                    let columna3 = document.createElement("td");
                    let columna4 = document.createElement("td");

                    columna1.textContent = nombre;
                    columna2.textContent = pelicula;
                    columna3.textContent = textoHorario;
                    columna4.textContent = textoExtra;

                    fila.append(columna1);
                    fila.append(columna2);
                    fila.append(columna3);
                    fila.append(columna4);

                    document.querySelector("#tbody").append(fila);
            }
            ```

    ??? example "Ejercicio 19: Registro de Viajes"
                
        === "index.html"

            ```html
            <!DOCTYPE html>
            <html lang="es">
                <head>
                    <meta charset="UTF-8">
                    <title>Registro de viajes</title>
                    <script src="app.js" defer></script>
                    <link rel="stylesheet" href="estilos.css">
                </head>
                <body>
                    <h2>🌍 Registro de viajes</h2>

                    <div class="formulario">

                        <div class="campo">
                            <label for="destino">Destino</label>
                            <input type="text" id="destino" name="destino">
                        </div>

                        <div class="campo">
                            <label for="transporte">Transporte</label>
                            <select id="transporte" name="transporte">
                                <option value="Coche">Coche</option>
                                <option value="Tren">Tren</option>
                                <option value="Avión">Avión</option>
                            </select>
                        </div>

                        <div class="campo">
                            <label for="precio">Precio</label>
                            <input type="text" id="precio" name="precio">
                        </div>

                        <div class="campo">
                            <label>Tipo de viaje</label>
                            <div class="opciones">
                                <label><input type="radio" name="tipoViaje" class="tipoViaje" value="Ida"> Ida</label>
                                <label><input type="radio" name="tipoViaje" class="tipoViaje" value="Ida y vuelta"> Ida y vuelta</label>
                            </div>
                        </div>

                        <div class="campo">
                            <label>Servicios adicionales</label>
                            <div class="opciones">
                                <label><input type="checkbox" name="extras" class="extra" value="Hotel"> Hotel</label>
                                <label><input type="checkbox" name="extras" class="extra" value="Guía"> Guía</label>
                            </div>
                        </div>

                        <button onclick="agregar()">Añadir viaje</button>

                    </div>

                    <table id="tabla">
                        <thead>
                            <tr>
                                <th>Destino</th>
                                <th>Transporte</th>
                                <th>Tipo</th>
                                <th>Precio</th>
                                <th>Extras</th>
                            </tr>
                        </thead>
                        <tbody id="tbody">
                        </tbody>
                    </table>
                </body>
            </html>
            ```
        
        === "estilos.css"

            ```css
            body {
                font-family: Arial, sans-serif;
                margin: 0;
                padding: 0;
                color: white;
            }

            h2 {
                text-align: center;
                margin-top: 20px;
            }

            /* TARJETA */
            .formulario {
                background: white;
                color: black;
                width: 420px;
                margin: 30px auto;
                padding: 25px;
                border-radius: 12px;
                box-shadow: 0 10px 25px rgba(0,0,0,0.3);
            }

            /* CAMPOS */
            .campo {
                margin-bottom: 15px;
            }

            label {
                display: block;
                font-weight: bold;
                margin-bottom: 5px;
            }

            /* INPUTS */
            input[type="text"], select {
                width: 100%;
                padding: 8px;
                border-radius: 6px;
                border: 1px solid #ccc;
            }

            /* OPCIONES */
            .opciones {
                display: flex;
                gap: 15px;
            }

            /* BOTÓN */
            button {
                width: 100%;
                padding: 10px;
                background: #0284c7;
                border: none;
                border-radius: 6px;
                color: white;
                font-size: 16px;
                cursor: pointer;
            }

            button:hover {
                background: #0369a1;
            }

            /* TABLA */
            table {
                width: 85%;
                margin: 20px auto;
                border-collapse: collapse;
                background: white;
                color: black;
                border-radius: 8px;
                overflow: hidden;
            }

            th {
                background: #0284c7;
                color: white;
            }

            td, th {
                padding: 10px;
                border: 1px solid #ddd;
                text-align: center;
            }
            ```

        === "app.js"

            ```javascript
            // TAREAS A REALIZAR:
                //1 Recuperar todos los valores introducidos por teclado: destino, transporte, textoTipo, precio y textoExtra.
                //  Además se deberá añadir al precio del viaje una comisión en funciónn de la variable textoTipo.
                //  Si textoTipo es igual a "Ida" se sumará al precio del viaje 1000 €, sino, se sumarán 1500 €.

                //2 Crear una fila donde aparezcan todos los valores introducidos por teclado más el 'precio' calculado.
                // Cada valor se mostrará en una columna y las  columnas se añadirán a la fila creada.
                
                //3 Añadir la fila a la tabla creada en el fichero html

                //4 Borrar los datos introducidos por el usuario.

            function agregar() {
                let i = 0, textoTipo = "", textoExtra = """;
                let precio = 0;

                // ESCRIBIR EL CÓDIGO NECESARIO
            }
            ```

        === "SOLUCIÓN"

            ```javascript
            function agregar() {
                let i = 0, textoTipo = "", textoExtra = "";
                let precio = 0;

                let destino = document.querySelector("#destino").value;
                let transporte = document.querySelector("#transporte").value;
                precio = parseFloat(document.querySelector("#precio").value);

                let tipo = document.querySelectorAll(".tipoViaje");
                while (i < tipo.length) {
                    if (tipo[i].checked) {
                        textoTipo = tipo[i].value;
                    }
                    i++;
                }

                if(textoTipo === "Ida")
                {
                    precio = precio + 1000
                }
                else{
                    precio = precio + 1500;
                }

                i = 0;
                let extras = document.querySelectorAll(".extra");
                while (i < extras.length) {
                    if (extras[i].checked) {
                        textoExtra = textoExtra + " - " + extras[i].value;
                    }
                    i++;
                }

                let fila = document.createElement("tr");
                let columna1 = document.createElement("td");
                let columna2 = document.createElement("td");
                let columna3 = document.createElement("td");
                let columna4 = document.createElement("td");
                let columna5 = document.createElement("td");

                columna1.textContent = destino;
                columna2.textContent = transporte;
                columna3.textContent = textoTipo;
                columna4.textContent = precio;
                columna5.textContent = textoExtra;

                fila.append(columna1);
                fila.append(columna2);
                fila.append(columna3);
                fila.append(columna4);
                fila.append(columna5);

                document.getElementById("tbody").append(fila);
            }
            ```
    
